import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bellatrix.shyftstarter',
  appName: 'ShyftStarter',
  webDir: 'dist'
};

export default config;
