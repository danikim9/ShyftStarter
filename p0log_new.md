# ShyftStarter 프로토타입 빌드 로그

**최초**: 2026-08-30 (Employee App P0) · **업데이트 1**: Manager Dashboard 추가 · **업데이트 2**: P1(컨디션 체크인/마이크로러닝/Role-play) · **업데이트 3**: Progress·Team 탭 · **업데이트 4**: Executive Dashboard · **업데이트 5**: v2 Shift Companion 피벗 리빌드 · **업데이트 6**: 컨디션 체크인 이모지 교체 · **업데이트 7**: 소셜 로그인 데모 UX + 캘린더 내보내기 · **업데이트 8**: 팀 참여(초대 코드/링크) 플로우 · **업데이트 9**: My Shift 화면 순서 재배치 + 휴무일 표시 · **업데이트 10**: Manager Dashboard 근무 일정 관리 탭 · **업데이트 11**: Manager Dashboard 매장 전환 드롭다운 예시 · **업데이트 12**: My Shift 팀원 일정 + 근무 교대(Shift Swap) 요청 · **업데이트 13**: 솔로 리텐션 갭 보완 + 개인 알람/리마인더 + 셀프 근무 입력 · **업데이트 14**: 근무 교대 승인 주체를 매니저 → 상대 팀원으로 전환 · **업데이트 15**: My Actions 완료 체크 되돌리기(언체크) · **업데이트 16**: 동료 그룹(Crew) 생성/참여 — 매니저 없이도 직원끼리 팀 꾸리기 · **업데이트 17**: 동료그룹 코드 Team 탭 이동 + 공지 작성 권한 전체 확대 + 로그인 온보딩 Team 탭 통합 · **업데이트 18**: 매니저 PRO 참여 코드 커스터마이즈 + 솔로 UX 플로우 리뷰 · **업데이트 19**: 솔로 UX 리뷰 피드백 전면 반영 — 예상 급여 계산기·월간 캘린더·반복 근무 등록·습관 그래프 + 마찰 지점 5건 수정 · **업데이트 20**: My Actions로 급여계산기 이동 + 하단 탭 재배치 + Team 공지/인수인계 확인·히스토리 + My Shift 인수인계 배너 접기 + 온보딩 랜딩 화면

## 참고 자료 (업로드됨)
- `Shyftstarter_CTO.pdf` — 제품 스펙 & 개발자 핸드오프 (27p): 페르소나(07p), 화면별 Feature Spec, IA(08p),
  데이터 모델(22p), AI 6-Layer 아키텍처(23p), Privacy/Governance(24p), MVP Scope P0/P1/P2(25p),
  Acceptance Criteria(26p), Manager Dashboard(19p)/Employee Detail(20p)/Quest Creation(21p), Learn/Role-play(18p)
- `Bellatrix_CFO_v.5.pdf`, `Bellatrix_사업계획서.pdf` — 피치덱: ROI 스토리, Capability Stat Wheel, GTM,
  **"The Smoking Gun" 체크리스트 이수율 vs ATV 상관관계 차트(7p, r 상관·+15.4%)**
- 코칭 레퍼런스 4종(Samsung 필드 코칭 매뉴얼 등) — **참고만 하고 특정 브랜드/산업 색깔을 뺀 채
  한국어로 직접 재구성**해서 사용 (사용자 명시 요청)

## 1차: Employee App P0 (결정 사항)
1. 범위: Employee 앱 P0 전체 (Home/Schedule/Quests/Stats/Coach)
2. 기술 스택: React + Vite + TypeScript, Tailwind v4 + Recharts
3. 데이터: 목업 데이터로 시작 (`src/data/mockData.ts`), 백엔드 미연동
4. AI 로직: 규칙 기반 mock (`src/lib/aiEngine.ts`) — CTO 문서의 Structured JSON 패턴을 따르도록 설계해
   나중에 실제 Claude API로 교체해도 화면 코드는 안 바뀜
5. Stats 화면에 **바그래프 + 밸런스휠(레이더 차트)** 동시 표시 (사용자 명시 요청)

데모 페르소나: 지은 (Sales Associate, 강남점) — 스킬 8종 × 5시프트 히스토리. Killer Script는
스킬당 2개 버전을 SHOW ANOTHER로 순환.

## 2차: Manager Dashboard (사용자 확인: "매니저 대시보드로 가줘")
- CTO 문서 Screen 19-21을 그대로 구현: **팀 현황**(오늘 코칭 필요 직원 카드 + 전체 로스터),
  **Will × Capability 매트릭스**(산점도, 4분면: 스타 플레이어/성장형 인재/몰입 저하/즉각 개입 필요),
  **직원 상세 패널**(Capability 바+레이더 재사용/Activity/AI 3단 요약/액션 버튼),
  **퀘스트 생성 모달**(AI가 약점 스킬 기준 초안 자동 생성), **1:1 코칭 가이드 모달**
- 팀 목업 6명 (`src/data/team.ts`): 지은(성장형), 박준서·이서연(스타), 최민경(몰입 저하 — 역량은 최상위인데
  참여도 급락), 김도현(즉각 개입 — 신입 2개월), 한소미(성장형 — 빠른 성장세). "3명 주목 필요"로 CTO 문서
  Screen19 예시와 정확히 일치하도록 설계
- 코칭 콘텐츠 라이브러리 (`src/data/coachingContent.ts`): 서브에이전트로 4개 레퍼런스 문서를 조사해
  6단계 응대 프레임워크, Killer Script 추가 버전, 제네릭 Before/After 스크립트, 체크리스트 문구 풀,
  매장 문화 원칙 8종, 매니저 1:1 대화 템플릿(5단계)을 추출 — **모두 한국어로 재작성하고 브랜드/산업
  특정 요소를 제거**해서 예시 콘텐츠로만 사용. 실제 고객사 온보딩 시 그 회사 SOP로 교체하는 자리.

## 3차: P1 진행 (사용자 확인 사항 반영)
1. **Will 점수에 컨디션 체크인 추가**: 사용자가 "쉬프트 전 감정/컨디션 체크(하트, 1초 내 응답)"를
   Will 점수 신호로 추가해달라고 요청 → Employee 앱에 시프트 시작 시 뜨는 1탭 하트(1~5) 체크인 팝업
   구현(`src/components/MoodCheckIn.tsx`, 스킵 가능), Home에 오늘 체크인 결과 표시, Manager의 직원
   상세 패널에 "WILL(참여도) 근거" 카드로 최근 5시프트 하트 히스토리 + willBasis 텍스트 노출.
   Will×Capability 매트릭스 설명 문구도 컨디션 체크인을 명시하도록 갱신.
   (※ 업데이트 6에서 하트 → 표정 이모지로 교체됨. 아래 참고)
2. **4분면 임계값(65점)은 임의값 유지** — 향후 실데이터로 조정 필요하다는 코멘트는 코드/README에 남겨둠.
3. **P1 진행**: CTO 문서 18p 스펙대로 두 기능 구현.
   - **마이크로러닝**: `src/data/learningContent.ts`(스킬 8종 × 모듈), AI가 스킬 갭 기준 1개 추천 →
     Why/Duration/Outcome → 시작 시 실전 팁 3개 → 완료 시 XP. Home 퀵액션 3번째 버튼 + Coach 화면에서 진입.
   - **AI Role-play(텍스트 전용)**: 스킬별 고객 시나리오(`ROLE_PLAY_SCENARIOS`) + 텍스트 응답 →
     `scoreRolePlayResponse()`(규칙/키워드 기반 mock 채점, 공감·응답구조·가치전달·이의처리·클로징 5축) →
     점수 + 개선 팁 + TRY AGAIN. Coach 화면 "START ROLE-PLAY" 버튼에 연결(이전엔 토스트 스텁이었음).

## 4차: Progress · Team 탭 (사용자 확인: "프로그레스, 팀리더보드 만들어줘")
CTO 문서 08p IA의 P1/P2 확장 네비게이션(LEARN·ROLE-PLAY·PROGRESS·TEAM)에서 남아 있던 두 항목을 구현.
Bottom Nav를 5탭 → 7탭으로 확장(Home/Schedule/Quests/Stats/Coach/**Progress**/**Team**).

- **Progress** (`src/screens/Progress.tsx`, `src/data/progressData.ts`): Stats(현재 스냅샷)와 구분되는
  "시간에 따른 성장" 화면. 연속 활동 스트릭·최장 기록·누적 퀘스트/학습 완료 스탯 타일 → 주간(8주)/월간(6개월)
  토글이 달린 Capability Trend 라인 차트 + AI 한줄 요약(`generateProgressInsight()`, 팀 평균 성장 속도
  대비 비교) → 스킬 8종을 변화량(Δ) 칩으로 정렬해 탭하면 해당 스킬의 성장 곡선을 확인하는 Skill Trajectory
  → Milestones 배지 그리드. 모든 추세 데이터는 결정론적 ramp 함수로 생성해 항상 Stats 화면의 실제 현재
  점수로 수렴하도록 설계(두 화면 숫자가 서로 어긋나지 않음).
- **Team** (`src/screens/Team.tsx`, `src/data/teamFeedData.ts`): Team Challenge(매장 공유 목표 진행바 +
  AI 응원 메시지) → Recognition(매니저/동료/AI 인정 피드) → Leaderboard(역량 종합 점수 순위, `team.ts`의
  6명 재사용). CTO 문서의 **"Leaderboard (기업별 On/Off)"** 스펙을 화면 내 토글로 시연 — 꺼두면 안내 문구만
  노출.
  - **설계 판단**: 매니저 대시보드의 willScore·needsAttention·aiSummary 등은 동료가 서로 볼 수 있으면
    "감시" 프레임이 되므로, 리더보드에는 이름·역할·역량 종합 점수만 노출하고 참여도/코칭 신호는 절대
    포함하지 않음 (`teamFeedData.ts`에 명시).

## 5차: Executive Dashboard (사용자 확인: "이그제큐티브 대시보드")
CTO 문서 07p Persona 3(Executive/Admin)를 신규 세 번째 페르소나로 구현. 상단 스위처가
Employee App / Manager Dashboard / **Executive Dashboard** 3개로 확장(`App.tsx`), Manager Dashboard와
동일한 사이드바 + 2뷰 구조(`src/exec/ExecutiveDashboard.tsx`)를 재사용.

- **조직 현황** (`src/exec/OrgOverview.tsx`): Organization Performance KPI 8종, 지역별(서울/경기/부산)
  매장 비교 테이블(`src/exec/components/StoreTable.tsx`), 조직 전체 Capability Map 레이더(플랫폼 도입
  전 대비 현재, `ORG_CAPABILITY_MAP`) — 클로징·크로스셀이 조직 전체에서 가장 약한 스킬이라는 결론으로
  트레이닝 우선순위 근거 제시.
- **ROI 분석** (`src/exec/RoiAnalysis.tsx`): **"The Smoking Gun"** — CFO 피치덱 7p를 재현한 체크리스트
  이수율×ATV 산점도 + 회귀선(`src/exec/components/CorrelationChart.tsx`). Training ROI(트레이닝 이수율
  상위/하위 매장 그룹 비교), Coaching ROI(코칭 세션 빈도 ↔ 역량 점수 상관계수), Brand Alignment(개념
  지표 — P2, 정의 미확정 명시).
- **핵심 설계 판단 — 진짜 통계 계산**: `src/lib/execAnalytics.ts`에 Pearson 상관계수·선형회귀·상위 20%
  uplift % 함수를 직접 구현. `src/data/execData.ts`의 10개 매장 목업(체크리스트 이수율 X, ATV Y)을
  파이썬으로 사전 튜닝해 실제 계산 결과가 프로젝트 비전 문서가 인용한 **r≈0.74·+15.4%**에 근접하도록
  맞췄다 — 화면에 보이는 숫자는 하드코딩이 아니라 매 렌더링마다 계산되므로, 나중에 실데이터로 바꿔도
  로직은 그대로 재사용됨. 강남점 집계는 `team.ts`의 실제 6명 평균과 일치(역량74·참여도70·이수율69%·
  CVR24%·ATV 120,667원)해서 세 페르소나 화면의 숫자가 서로 어긋나지 않음.
- **핵심 설계 판단 — 프라이버시/RBAC 일관성**: CTO 문서 24p RBAC 표(Regional/Enterprise Admin은
  Region/Company-wide 데이터만 접근)를 그대로 반영해, Executive Dashboard는 **개별 직원 이름·점수를
  전혀 노출하지 않고 매장/조직 단위 집계만** 보여줌 — Team 탭 리더보드 설계 때와 같은 원칙(민감한
  개인 데이터는 상위 권한이라도 함부로 노출하지 않는다)을 조직 계층에도 일관되게 적용.

## 6차: v2 "Shift Companion" 피벗 리빌드 (사용자 확인: 전략 문서 확정 후 "바로 앱 리빌드 시작")

**배경**: 기능이 과다해져 심플화가 필요하다는 사용자 판단 + 처음부터 B2B가 아니라 개인 교대근무자용
B2C 앱으로 시작해 유료 구독으로 확장하고, 이후 B2B(브랜드 커스터마이즈·매장별 대시보드)를 얹는 방향으로
사업 방향을 조정. 사용자가 직접 핵심 정의("Schedule → Shift → Action → Growth" 퍼널, "Shift
Companion" 포지셔닝)와 MVP 5개 기능, 숨길 기능 목록, 4단계 수익화 사다리(Free-Worker/Team-Manager/
Business-SMB/Enterprise-Brand), Team 탭을 완전한 메신저로 만들지 않는다는 결정, "알바생 한 명이 아무도
초대하지 않은 상태에서도 왜 설치하고 다시 열까"라는 북극성 질문을 제시 → Claude가 이를 전략 문서로
정리(`claude/shyftstarter-v2-strategy-b2c-pivot.md`, "ShyftStarter v2 — Shift Companion 전략 (확정)")한
뒤 승인받아 리빌드 진행.

- **Employee App 3탭으로 축소**: My Shift / Team / My Actions (구 Home/Schedule/Quests/Stats/Coach/
  Progress/Team-리더보드는 nav에서 숨기되 코드는 그대로 유지 — "hide, don't delete" 원칙)
- **My Shift** (`src/screens/MyShift.tsx`): 오늘 근무 히어로 카드 → 상세(오늘 할 일 보기/인수인계
  남기기), 최근 인수인계 미리보기, 예정/지난 근무 리스트, 컨디션 체크인 뱃지
- **Team** (`src/screens/TeamFeed.tsx`): Announcement(공지, 고정 가능·리액션·댓글) + Handover(인수인계)
  피드 통합. **의도적으로 완전한 메신저를 만들지 않음** — 실시간 잡담은 카카오톡 등 기존 채팅앱 영역으로
  남기고, 시프트에 묶인 구조화된 정보만 소유. 개발 비용 절감 + 기존 채팅앱과의 직접 경쟁 회피라는 이중
  효과
- **My Actions** (`src/screens/MyActions.tsx`): 셀프/매니저/AI 통합 `Action` 엔티티(기존 별개였던
  Quest+Checklist 개념 통합, `src/types/index.ts`). 완료 시 토스트 + 주간 완료 카운터만 노출 — XP/레벨은
  의도적으로 숨김(최소 완료 피드백 장치만 유지)
- **Invisible AI**: `generateQuickActions()`(`src/lib/aiEngine.ts`) — 자연어 프롬프트를 키워드 매칭해
  체크리스트 문구 3~4개 제안. AI라는 라벨을 전면에 내세우지 않는 "그냥 빠른" 경험으로 설계
- **ActionEvent 조용한 로깅**: 완료할 때마다 화면에 보이지 않는 이벤트가 기록됨 — 향후 Employee
  Performance Graph 데이터 모델의 씨앗, UI 변경 없이 나중에 활용 가능
- **Manager Dashboard에 "팀 액션 · 공지" 화면 추가** (`src/manager/TeamActionsComposer.tsx`, 신규
  기본 화면): 공지 등록 + 팀 액션 배포 폼. `AppStateProvider`가 이미 세 페르소나를 앱 전역에서
  감싸고 있다는 기존 아키텍처를 활용해, 매니저가 배포한 공지/액션이 Employee App의 Team/My Actions
  탭에 **같은 React 상태로 즉시 반영**되도록 구현(백엔드 없이 실시간 데모 가능)
- **핵심 설계 판단 — 타입 하위호환 유지**: `TabId`/`SheetKind` 유니온을 기존 값+신규 값의 합집합으로
  유지해, 숨겨진 구 화면들(Stats/Coach/Progress/구 Team/Executive)이 nav에서만 제거되고 타입 체크는
  계속 통과하도록 설계 — 나중에 상위 티어에서 다시 연결할 때 코드 재작성 불필요
- **README.md 전면 개편**: v2 MVP를 최상단 설명으로, 기존 P0~P2 문서는 "숨겨진 기능(Business+/
  Enterprise 참고용)" 섹션으로 이동(삭제하지 않음)

## 7차: 컨디션 체크인 — 하트 → 표정 이모지 교체 (사용자 확인: "감정 물어보는거 감정이모티콘으로 바꿔주라 하트 말구")

체크인 UX 자체(1탭·스킵 가능)는 그대로 두고, 표현 방식만 하트(❤️/🤍) 채움 방식에서 값별 고유 표정
이모지로 교체.

- `src/components/MoodCheckIn.tsx`: `MOOD_EMOJI` 맵 신설(1→😣 많이 힘들어요, 2→😕 조금 힘들어요,
  3→😐 보통이에요, 4→🙂 괜찮아요, 5→😄 아주 좋아요) 및 `export`. 체크인 팝업의 5개 버튼이 하트 채움
  대신 각자 고유 이모지를 항상 보여주고, hover 시 스케일업+비선택 항목 반투명 처리로 포커스 표시.
  안내 문구도 "하트 하나만 눌러주세요" → "표정 하나만 눌러주세요"로 수정
- `src/screens/Home.tsx`, `src/screens/MyShift.tsx`: 헤더의 "오늘 컨디션 체크인" 뱃지가
  `'❤️'.repeat(todayMood)` 대신 `MOOD_EMOJI[todayMood]` 단일 이모지로 표시
- `src/manager/EmployeeDetailPanel.tsx`: WILL(참여도) 근거 카드의 최근 5시프트 히스토리 스트립도
  하트 채움/빈 하트 대신 시프트별 `MOOD_EMOJI` 표정으로 표시
- `MOOD_EMOJI`를 `MoodCheckIn.tsx`에서 단일 소스로 export해 Home/MyShift/EmployeeDetailPanel이
  모두 재사용 — 나중에 이모지 세트를 바꿀 때 한 곳만 수정하면 됨
- `tsc --noEmit` 클린, `npm run build` 성공 확인 후 Artifact 재배포

## 8차: 소셜 로그인 데모 UX + 캘린더 내보내기 (사용자 확인: "계정을 네이버/구글/애플/카카오 로그인 연동... 일정은 외부 캘린더랑 동기화될 수 있도록 내보내기 기능 추가할 수 있어?")

**범위 확정 방식**: 두 요청 모두 "진짜로 될 수 있는 것"과 "지금 프로토타입에서 불가능한 것"을 먼저
구분해서 AskUserQuestion으로 확인받은 뒤 진행. 실제 OAuth 소셜 로그인은 각 사(네이버/카카오/구글/애플)
개발자 계정·API 키·백엔드 토큰 처리가 필요해 프론트엔드 전용 프로토타입 범위 밖 — 대신 로그인 화면
UX(데모 흐름)만 구현하기로 확정. 캘린더는 (1) 실제 동작하는 .ics 파일 내보내기(가능, 지금 구현)와
(2) 실시간 양방향 자동 동기화(백엔드+각 사 Calendar API 필요, 범위 밖)로 나눠서 (1)로 확정.

- **로그인 데모 화면** (`src/auth/LoginScreen.tsx`, 신규): 네이버(#03C75A)/카카오(#FEE500)/Google(흰
  배경)/Apple(검정) 4개 브랜드 컬러 버튼. 실제 로고 아트워크 재현 대신 브랜드 컬러 + 제네릭 마크(N
  레터배지, 말풍선, 4색 점, 심플 사과 실루엣)로 상표권 이슈 없이 인지 가능하게 구성. 버튼 클릭 시
  `onLogin(provider)` 호출 → 실제 인증 없이 바로 앱 진입 시뮬레이션. 나중에 실제 OAuth를 붙일 때
  `handleLogin` 내부 구현만 교체하면 되고 화면/UX는 그대로 재사용 가능하도록 설계.
- **`App.tsx`에 로그인 게이트 추가**: `authProvider` state가 없으면 `LoginScreen`을 풀스크린으로
  렌더링, 로그인하면 기존 3-페르소나 앱으로 진입. 우상단에 작은 로그아웃 버튼(`LogoutButton`) 추가해
  데모 중 로그인 흐름을 다시 보여줄 수 있게 함. (※ 업데이트 9에서 로그인↔앱 사이에 "팀 참여" 단계가
  추가되며 상태 머신 구조로 재정리됨. 아래 참고. ※ 업데이트 17에서 그 "팀 참여" 단계가 다시 제거되고
  Team 탭 안으로 흡수됨.)
- **캘린더 내보내기** (`src/lib/calendarExport.ts`, 신규): `generateICS()`가 근무 일정을 표준
  iCalendar(RFC 5545) 텍스트로 직접 생성(외부 라이브러리 없음), `downloadICS()`가 Blob + `<a
  download>`로 실제 브라우저 다운로드를 트리거. My Shift의 "예정된 근무" 섹션 헤더에 "내보내기" 버튼
  추가. **Playwright로 실제 다운로드 이벤트까지 검증** — 생성된 .ics 파일이 유효한 RFC 5545 포맷이고
  근무 3건이 정확한 시작/종료 시각으로 들어가는 것 확인, 사용자에게도 샘플 .ics 파일을 직접 전달해
  본인 캘린더 앱으로 가져오기 테스트 가능하도록 함.
  - **알아둘 점 — Artifact 미리보기 iframe 제약**: claude.ai Artifact로 배포된 라이브 프리뷰는 뷰어
    샌드박스가 페이지發 다운로드(blob:/data: href 포함)를 차단해서, 그 링크에서는 버튼을 눌러도
    실제로 저장되지 않을 수 있음. `downloads` 캐퍼빌리티로 우회하는 것도 검토했으나 `.ics` 확장자가
    Artifact 다운로드 허용 목록(gif/png/jpg/webp/mp4/webm/txt/json/md/docx/pptx/epub/csv/ttf/html/
    svg/pdf)에 없어서 불가 — `.txt`로 우회하면 확장자가 달라져 캘린더 앱이 자동 인식을 못 하므로
    채택하지 않음. **로컬 실행(`npm run dev`)이나 실제 배포 환경에서는 정상 동작**하는, 프로덕션에
    쓸 수 있는 진짜 구현임.

## 9차: 팀 참여(초대 코드/링크) 플로우 (사용자 확인: "팀공지나 일정 업데이트를 받으려면 어떤 링크나 초대를 받아야 하는거지?" → "QR이 좋을거같긴한데 비즈니스 티어에 제공하는 것도 좋다. 더 적합한 방안으로 해줘")

전략 문서 §6-3의 열린 질문("팀 생성/초대 UX: 코드 vs QR vs 링크")에 대한 답. Claude가 코드/링크/QR
세 방식을 비교 제시(코드=링크가 본질적으로 같은 메커니즘이라 하나로 묶고, QR은 카메라 스캔 구현
공수 대비 지금 단계 ROI가 낮아 Business 티어로 미루는 안을 추천) → 사용자가 그 판단에 동의하고
"더 적합한 방안으로" 그대로 진행하라고 위임.

- **`STORE_CODE`를 실제로 검증하는 로직으로 승격**: 기존에 `mvpData.ts`에 있던 `STORE_CODE = 'GN-4821'`
  목업 상수(6차 리빌드 때부터 자리만 잡아둔 것)를 실제 입력값 검증에 연결. `STORE_NAME`, `STORE_JOIN_LINK`
  (코드를 담은 URL) 신규 추가.
- **`useAppState()`에 `hasJoinedTeam`/`joinTeam(code)` 추가** (`src/lib/store.tsx`): 대소문자 무관 비교,
  성공 시 토스트 + `hasJoinedTeam=true`. 기본값은 `false`— Free 개인 사용자는 팀에 참여하기 전까지
  공지/인수인계를 받지 않는다는 원칙을 상태로 명시.
- **로그인 → 팀 참여 → 앱 진입 3단계 온보딩** (`App.tsx`를 `stage: 'login' | 'join' | 'app'` 상태
  머신으로 재구성, `src/auth/JoinTeamScreen.tsx` 신규): 로그인 후 "근무하는 매장에 참여해보세요"
  화면에서 코드 입력 또는 "코드가 없으신가요? 나중에 할게요"로 스킵 가능 — 북극성 질문(솔로 사용자도
  가치를 느껴야 한다)을 온보딩 단계에서부터 지키도록 설계. (※ 업데이트 17에서 이 별도 단계는 제거되고
  Team 탭 안으로 흡수됨. 아래 참고)
- **재사용 가능한 `JoinTeamForm`** (`src/auth/JoinTeamForm.tsx`): 온보딩 풀스크린과, 나중에 다시
  참여하고 싶을 때 여는 바텀시트(`SheetKind`에 `joinTeam` 추가, `SheetHost.tsx`) 양쪽에서 동일
  컴포넌트 재사용.
- **`TeamFeed.tsx`에 미참여 상태 반영**: `hasJoinedTeam=false`면 공지/인수인계 목록 대신 "아직 참여한
  팀이 없어요" 빈 상태 카드 + "코드로 참여하기" 버튼(→ `joinTeam` 시트) 노출. 참여 즉시 기존 목업
  공지/인수인계가 그대로 나타남.
- **Manager Dashboard "팀 액션·공지"에 "팀 초대" 카드 추가** (`src/manager/TeamInvite.tsx` 신규):
  참여 코드 + 참여 링크를 각각 클립보드 복사 가능한 행으로 표시(Playwright로 `navigator.clipboard`
  실제 복사까지 검증). **QR 초대는 잠금 아이콘 + "Business 티어부터 제공돼요" 배지로 시각적으로만
  제공** — 실제 QR 생성/스캔 기능은 만들지 않고, 티어 사다리(Team→Business)를 자연스럽게 보여주는
  업셀 요소로만 배치. 사용자가 QR을 Business 티어로 미루자는 판단에 동의한 것을 그대로 반영.
- `tsc --noEmit` 클린, Playwright로 코드 입력 성공/실패, 스킵 후 빈 상태, 매니저 코드 복사까지 전체
  플로우 스크린샷 검증 후 Artifact 재배포.

## 10차: My Shift 화면 순서 재배치 + 휴무일 표시 (사용자 확인: "화면 흐름이 최근인수인계가 오늘 근무일정 다음에 나오고 그 바로 밑에 인수인계 입력하기가 있었으면 좋겠어. 그리고 하단에 예정 근무일정 보이는 화면에는 오프(휴무)인 날도 표시되게 해줘")

- **화면 순서 재배치** (`src/screens/MyShift.tsx`): 기존에는 "오늘 근무 히어로 카드 → 예정/지난 근무
  → 최근 인수인계"였던 흐름을 "오늘 근무 히어로 카드 → **최근 인수인계** → **인수인계 남기기 버튼** →
  예정된 근무 → 지난 근무"로 재배치. "오늘 일하는 데 필요한 정보"(오늘 근무 → 인수인계 확인/작성)를
  먼저 배치하고 "일정 확인"(예정/지난 근무)은 그 다음으로 미루는 정보 우선순위로 정리.
- **휴무일(off) 상태 신설** (`src/types/index.ts`): `ShiftStatus`에 `'off'` 추가. `src/data/mockData.ts`의
  `shifts` 배열에 오늘(8/31) 근무 바로 다음 휴무일과 그 다음 근무 사이, 이후 근무 뒤에 휴무일 1건씩
  총 2건의 목업 휴무 시프트(`start`/`end`는 빈 문자열) 추가.
- **`ShiftRow` 컴포넌트가 휴무일을 별도로 렌더링**: 시간 대신 "쉬는 날" 텍스트, 배지는 "휴무"(기본
  톤), 행 전체를 살짝 반투명(`opacity-60`) 처리해 근무일과 시각적으로 구분.
  `Schedule.tsx`/`ShiftDetailView.tsx` 등 다른 화면은 `shift.status`를 배타적 switch가 아닌 조건부
  렌더링으로만 다루고 있어 새 상태 추가로 인한 타입 에러 없음을 Grep으로 확인(실제 코드 변경은
  `MyShift.tsx`만).
- **캘린더 내보내기와의 정합성**: `handleExport`가 `status !== 'off'`로 필터링하도록 유지해, 시작/종료
  시각이 없는 휴무일이 잘못된 .ics 이벤트로 나가지 않도록 함(Playwright로 유효 VEVENT 개수 재검증).
- `tsc --noEmit` 클린, `npm run build` 성공, Playwright 스크린샷으로 순서/휴무일 표시 확인 후 Artifact
  재배포, 소스 재압축 전달.

## 11차: 경쟁 앱 대비 차별화 포인트 분석 (사용자 확인: "내 앱이 다른 앱들과 비교했을때 차별성과 강점이 뭔지 알려줘")

서브에이전트로 글로벌 5개(When I Work, Deputy, Homebase, Sling, 7shifts) + 국내 5개(시프티, 샤플,
알바몬 보스몬/크루몬, 워키도키, 알바미) 총 10개 경쟁 앱을 웹 조사(포지셔닝/가격, 개인 성장 기능 유무,
게이미피케이션, 인수인계 기능, 채팅 범위, AI 기능, 모니터링/GPS 여부 기준). 핵심 발견: **10개 중
어느 앱도 "근무자 개인"을 향한 자발적 성장/게이미피케이션 기능을 제공하지 않음** — "AI"/"참여도"를
내세운 기능은 전부 매니저용 분석·자동화 도구였고, 근무자 화면에는 없었음. 7shifts의 인수인계 유사
기능은 매니저 → 팀 공지 방송 구조로 확인되어 동료 간 P2P 인수인계가 아님. Deputy/Homebase/Sling/
When I Work/시프티/알바미 등 다수가 GPS 지오펜싱 또는 위치 기반 출퇴근 체크를 제공(감시 프레임 존재).
→ ShyftStarter의 (1) 근무자용 스킬 휠·레벨업(숨겨진 기능이지만 아키텍처에 존재), (2) 동료 간 P2P
인수인계(TeamFeed), (3) GPS/음성/얼굴인식 전면 배제, (4) 스킵 가능하지만 존재하는 팀 참여 온보딩으로
솔로 사용성 유지, (5) 체크리스트 이수율↔ATV 상관관계 기반 재무 ROI 증명(Executive Dashboard)이
겹치는 경쟁사가 없는 포지셔닝임을 확인 — 대화로 답변, 별도 코드/화면 변경 없음. 이 분석을 전략
문서(`v2-strategy-b2c-pivot.md`)에 별도 섹션으로 정리할지는 다음 대화에서 사용자 확인 예정.

## 12차: Manager Dashboard "근무 일정 관리" 탭 (사용자 확인: "매니저가 매니저 대시보드 좌측 메뉴에 팀원들 근무일정 조정하는 탭도 있으면 좋을거같은데. 근무를 배정하거나 변경하거나 등등 휴무도 확인하고")

Manager Dashboard 사이드바에 팀 전체 근무를 한 화면에서 배정·변경·휴무 확인할 수 있는 신규 탭 추가.

- **신규 데이터 모델** (`src/data/roster.ts`): 기존 `mockData.ts`의 `shifts` 배열은 지은(Employee App
  단일 페르소나) 한 명의 일정만 모델링하고 있어, 팀 6명 전체의 근무를 보여주려면 별도 데이터셋이
  필요했음. `memberId → date → RosterEntry({start,end} | 'off')` 구조로 이번 주(8/24~8/30)·다음 주
  (8/31~9/6) 2주치를 목업 — 지은 분량은 `mockData.ts`의 실제 `shifts`(완료/진행중/휴무/예정)와 정확히
  일치하도록 맞춰 두 데이터셋이 서로 어긋나지 않게 함.
- **`RosterView.tsx`** (신규, `src/manager/`): 팀원(행) × 요일(열) 그리드 테이블. 각 셀은 근무 시간
  칩("10:00–18:00") 또는 "휴무" 배지를 보여주고, 클릭하면 모달이 열려 시작/종료 시간을 조정하거나
  "휴무로 표시" 토글로 전환할 수 있음. 오늘 날짜 열은 강조 표시. 하단에 날짜별 "근무 인원" 합계 행 추가.
  이번 주/다음 주 전환은 상단 화살표 토글로.
- **`ManagerDashboard.tsx`/`managerStore.tsx`**: `ManagerView`에 `'roster'` 추가, 사이드바 2번째 항목
  (팀 액션·공지 다음, 팀 현황보다 위)으로 "근무 일정 관리"(CalendarDays 아이콘) 배치 — 공지 등록과
  함께 매니저가 매일 쓰는 두 화면을 상단에 두고, 분석성 화면(팀 현황/Will×Capability)은 아래로.
- **설계 판단 — Employee App과 실시간 연동은 하지 않음**: 팀 액션·공지(9차/6차)는 `AppStateProvider`
  전역 상태를 통해 매니저가 등록하면 Employee App에 즉시 반영되도록 설계했지만, 근무 일정은 지은
  1명만 Employee App 페르소나로 존재하는 프로토타입 구조상 "팀 전체 근무표"를 Employee App의 개인
  일정 화면 하나에 실시간으로 매핑할 방법이 없어 이번 스코프에서는 제외 — 매니저 화면 자체의
  배정/조회/휴무 확인 기능으로 한정. 실제 멀티유저 백엔드가 들어가면 이 로스터 데이터 모델이 진짜
  근무 배정 시스템의 기반이 될 수 있도록 구조만 잡아둠.
  **(※ 14차에서 이 제약이 해소됨. 아래 참고)**
- `tsc --noEmit` 클린, `npm run build` 성공, Playwright로 그리드 렌더링·셀 클릭→모달→시간 변경/휴무
  토글→저장 반영·주간 전환까지 스크린샷 검증 후 Artifact 재배포, 소스 재압축 전달.

## 13차: Manager Dashboard 매장 전환 드롭다운 예시 (사용자 확인: "매니저 대시보드에 지점별 관리가 가능하게 왼쪽 상단에 드롭다운 옵션 예시 하나만 만들어줘. 예를들어 강남점/잠실점 이런식으로")

여러 매장을 함께 관리하는 매니저를 위한 매장 전환 UX를 "예시 하나만" 보여달라는 요청 — 실제 두 번째
매장의 팀/근무/액션 데이터를 통째로 새로 만드는 대신, 전환 UX 자체를 진짜로 동작하게 만들고 강남점
외 매장은 정직하게 "준비 중" 상태로 처리하는 쪽으로 판단(9차 QR 초대 때의 "시각적으로 완성되어 있지만
잠긴 기능" 패턴과 동일한 접근).

- **`src/data/stores.ts`** (신규): `STORES` 배열 — 강남점(`STORE_ID`, `ready: true`, 기존 전체 데이터
  연결)과 잠실점(`st_jamsil`, `ready: false`, 데이터 없음) 2개 옵션. 실제 멀티 매장 지원 시
  `team.ts`/`roster.ts`/`mvpData.ts`를 storeId 기준으로 쪼개는 확장의 출발점이 되도록 구조만 잡음.
- **`useManagerState()`에 `selectedStoreId`/`setSelectedStoreId` 추가** (`src/lib/managerStore.tsx`,
  기본값 강남점).
- **`StoreSwitcher.tsx`** (신규, `src/manager/`): 사이드바 좌측 상단 로고 바로 아래 드롭다운 버튼 —
  현재 매장명 + 셰브런, 클릭하면 매장 목록이 펼쳐지고 각 항목에 체크 표시(선택됨)와 데이터 미비 매장은
  "데이터 준비 중" 라벨이 붙음.
  - **`ManagerDashboard.tsx`**: 잠실점처럼 `ready: false`인 매장을 선택하면 사이드바 네비게이션은
    그대로 두고 콘텐츠 영역 전체를 "잠실점 데이터는 아직 준비 중이에요" 안내 카드로 교체(어떤 탭을
    누르고 있었는지와 무관하게) — "강남점으로 돌아가기" 버튼 포함. 사이드바 하단 "Store Manager ·
    {매장명}" 텍스트도 선택된 매장에 따라 동적으로 바뀌도록 수정.
- `tsc --noEmit` 클린, `npm run build` 성공, Playwright로 드롭다운 열기 → 잠실점 선택 → 준비 중 화면
  → 강남점으로 돌아가기까지 스크린샷 검증 후 Artifact 재배포, 소스 재압축 전달.

## 14차: My Shift 팀원 일정 + 근무 교대(Shift Swap) 요청 (사용자 확인: "개인 홈화면에 내 근무일정 볼 수 있자나, 같은 팀원들 일정도 볼수있게 해줄수있어? 그리고 프로 기능으로 매니저 승인 하에 서로 근무일정 스왚 가능한 기능도 추가해줘. 이거는 실제로 구현안되고 프로기능으로 들어갈거라고 표기해도 좋아. 너가 판단했을때 기능 구현되는걸 보여주는게 좋을거같으면 실행해주고")

두 가지 요청 — (1) My Shift에서 같은 팀원들 일정도 보기, (2) 매니저 승인 하 근무 교대 요청을 "PRO"
기능으로 — 이었고, 사용자가 (2)는 "실제 구현 없이 라벨만"도 괜찮다고 명시하면서 동시에 판단에 따라
진짜로 구현해도 좋다고 위임. **실제로 동작하는 기능으로 구현하기로 판단** — 이유는 (a) 12차에서 이미
만들어 둔 팀 로스터 공유 상태 아키텍처를 그대로 재사용할 수 있어 비용이 낮고, (b) "매니저 코칭 오버헤드
감축"이라는 비전 핵심 가치를 실제 동작하는 워크플로로 보여주는 것이 피치덱/데모에서 정적인 잠금 화면보다
훨씬 설득력 있고, (c) 그럼에도 진입점에는 "PRO" 크라운 배지를 붙여 유료 티어 기능이라는 정직한 표기를
유지했기 때문.

- **핵심 아키텍처 변경 — 로스터를 공유 상태로 승격** (`src/lib/store.tsx`): 12차에서 `RosterView.tsx`
  내부 로컬 state였던 팀 로스터(`roster.ts`의 `INITIAL_ROSTER`)를 `AppStateProvider`(Employee/Manager/
  Executive 세 페르소나를 앱 전역에서 감싸는 기존 컨텍스트)로 끌어올림 — 팀 액션·공지(6차/9차)가 이미
  쓰고 있던 것과 같은 패턴. `roster`/`updateRosterEntry` 상태·액션 추가, `RosterView.tsx`는 로컬
  `useState` 대신 `useAppState()`의 공유 로스터를 그대로 사용하도록 리팩터 — 이제 Manager가 근무를
  배정/변경하면 같은 상태를 Employee App도 즉시 볼 수 있음.
- **근무 교대 요청 데이터 모델** (`src/types/index.ts`): `SwapRequest`(요청자/대상자/각자의 근무 날짜/
  선택 메모/상태 pending·approved·rejected) 신규 타입. `src/lib/store.tsx`에 `swapRequests` 상태와
  `requestSwap`/`approveSwap`/`rejectSwap` 액션 추가. `approveSwap`은 두 사람의 해당 날짜 `RosterEntry`
  값을 실제로 맞교환(요청자의 원래 근무는 요청자 쪽에서 휴무 처리되고 대상자의 원래 근무 시간이
  요청자의 대상 날짜에 들어가며, 대상자 쪽도 대칭으로 처리)하도록 구현 — Playwright로 날짜·시간이
  모두 다른 두 근무를 교대시켜 정확히 반대로 스왑되는 것까지 검증 완료.
- **`src/components/sheets/TeamScheduleView.tsx`** (신규): My Shift에서 여는 바텀시트. 이번 주/다음 주
  토글 + 요일 칩으로 날짜를 고르면 팀원 6명 전원의 그날 근무/휴무가 한 리스트로 보임(지은 본인 행은
  강조 표시). 본인의 예정된(오늘 이후) 근무를 선택하면 "이 근무 교대 요청하기" 버튼(PRO 크라운 배지)이
  나타나고, 누르면 3단계 인라인 폼(교대할 팀원 선택 → 그 팀원의 예정 근무 중 하나 선택 → 메모 입력 후
  전송)으로 이어짐. 전송한 요청들의 상태(대기 중/승인됨/거절됨)도 같은 화면 상단에 표시.
- **`src/manager/RosterView.tsx`**: 그리드 위에 대기 중인 교대 요청 카드(`SwapRequestsCard`, PRO 배지)
  신규 추가 — 요청자↔대상자·날짜·선택 메모를 보여주고 승인/거절 버튼 제공. 승인 시 로스터 그리드가
  즉시 갱신되는 것을 Playwright로 확인.
- **연결 배선**: `src/components/sheets/SheetHost.tsx`에 `teamSchedule` 시트 종류 추가,
  `src/screens/MyShift.tsx`의 "예정된 근무" 섹션 헤더에 "내보내기" 버튼 옆으로 "팀원 일정" 진입 버튼
  추가.
- **알아둘 점 — 아직 남은 데이터 소스 분리**: 12차 설계 결정대로 `roster.ts`(팀 전체 그리드용)와
  `mockData.ts`의 `shifts` 배열(지은 개인 홈 화면의 "예정된 근무"/"지난 근무" 목록과 .ics 캘린더
  내보내기가 쓰는 소스)은 여전히 서로 다른 데이터셋임. 즉 교대가 승인되면 **팀원 일정 시트와 매니저
  로스터 그리드에는 정확히 반영**되지만, My Shift 메인 화면의 근무 목록과 캘린더 내보내기 파일은
  `mockData.shifts`를 그대로 쓰기 때문에 **교대 결과가 반영되지 않음**. (※ 셀프 근무 입력은 15차에서
  같은 `roster` 소스를 재사용하고, 이 seam 자체는 여전히 남아 있음 — 아래 참고.)
- `tsc --noEmit` 클린, `npm run build` 성공. Playwright로 (1) 같은 날짜 간 교대(양쪽 다 동일 시간이라
  시각적으로 무변화지만 로직상 정상 교환 확인) (2) 서로 다른 날짜·시간 간 교대(지은 9/1 10:00–18:00 ↔
  최민경 8/31 14:00–22:00) — 요청 전송 → 매니저 승인 → 양쪽 그리드가 정확히 교차 반영되는 것까지
  전체 플로우 스크린샷 검증 후 Artifact 재배포, 소스 재압축 전달.

## 15차: 솔로 리텐션 갭 보완 + 개인 알람/리마인더 + 셀프 근무 입력 (사용자 확인: "방금 나온 피드백과 문제점을 모두 보완해서 다시 웹앱 만들어줘. 솔로리텐션 갭과 개인알람/리마인더 기능을 바로 구현하는거야." → "아 그리고, 매니저가 일정을 부과하지 않았을때 아직 매니저가 미사용자일때를 대비해 솔로 또는 팀원들끼리도 일정공유가 가능하도록 자체 스케쥴 기입도 가능하게 해줘.")

**배경**: 대화 중 "개인용으로도 리텐션을 충분히 올릴 수 있는 앱인지" 질문에 CPO 관점에서 답하며 두 가지
문제를 짚었다 — (1) 14차의 "팀원 일정" 기능이 실제로는 팀 참여가 전제된 기능인데 `hasJoinedTeam` 체크
없이 항상 열려 있어, 솔로(미참여) 사용자에게는 오히려 의미 없는 진입점으로 보일 수 있었다는 점.
(2) My Actions의 완료 피드백이 "이번 주 N회"뿐이라 북극성 질문(솔로 사용자가 왜 다시 여는가)에 대한
답이 되기엔 약하고, 전략 문서에 있었지만 미착수였던 "개인 알람/리마인더"가 사실 가장 우선순위 높은
갭이라는 점. 사용자가 이 두 가지를 바로 구현하라고 확정한 뒤, 추가로 "매니저가 아직 이 앱을 쓰지 않는
상황에서도 직원이 스스로 근무를 입력해 자신은 물론 팀원과도 공유할 수 있어야 한다"는 세 번째 요구를
얹었다 — 이는 사실 §6의 북극성 질문과 MVP 정의("My Shift: 내 근무 일정 확인/**입력**")에 이미 있었지만
지금까지 "확인"만 구현되고 "입력"은 비어 있던 부분이라, 세 요구가 결과적으로 하나의 정합적인 개편으로
이어졌다.

- **① "팀원 일정" 시트를 솔로/팀 이중 모드로 재설계** (`src/components/sheets/TeamScheduleView.tsx`
  전면 개편): 단순히 `hasJoinedTeam`으로 숨기는 대신, **팀 미참여 상태에서도 진짜로 쓸모 있는 화면으로
  바꿨다** — `!hasJoinedTeam`이면 본인 근무 한 줄만 보여주고(팀원 목록은 노출하지 않음), 근무 교대
  버튼도 숨기고 "팀에 참여하면 동료 일정도 보고 서로 맞바꿀 수 있어요 · 참여하기" 안내 카드를 비침습적
  업셀로 배치. `hasJoinedTeam`이면 기존 14차의 팀 전체 리스트 + 교대 요청이 그대로 나옴. My Shift의
  버튼 라벨도 "팀원 일정" → "근무 일정"으로, 아이콘도 `Users` → `CalendarDays`로 바꿔 두 모드를 모두
  아우르는 이름으로 정리(시트 타이틀도 SheetHost.tsx에서 동일하게 변경).
- **② 셀프 근무 입력** (같은 파일 + `src/manager/RosterView.tsx`의 `EditShiftModal`/`EditTarget`을
  `export`로 승격해 재사용): 솔로/팀 두 모드 모두에서 본인 행(연필 아이콘)을 탭하면 매니저가 쓰던 것과
  똑같은 시간/휴무 편집 모달이 뜨고, 저장하면 `updateRosterEntry(CURRENT_EMPLOYEE_ID, date, entry)`를
  그대로 호출한다 — 14차에서 로스터를 이미 `AppStateProvider` 공유 상태로 올려둔 덕분에 매니저 쪽
  코드를 건드리지 않고 재사용만으로 구현됐다. **매니저가 앱을 전혀 쓰지 않은 상태에서도** 직원이 자기
  근무를 입력하면, 나중에 매니저가 Manager Dashboard "근무 일정 관리"를 처음 열어도 이미 채워진
  일정을 보게 되는 것까지 Playwright로 실제 확인(솔로 상태로 9/2 근무를 09:00–13:00로 입력 → 이후
  Manager Dashboard로 전환해 같은 값과 갱신된 "근무 인원" 수를 그대로 확인). 다른 팀원의 행은 편집
  불가(조회 전용)로 남겨 "본인 근무만 스스로 관리"라는 원칙을 지켰다.
- **③ 솔로 리텐션 피드백 강화** (`src/screens/MyActions.tsx`, `src/lib/store.tsx`): 기존 "이번 주 N회
  완료" 타일 옆에 "연속 N일째" 스트릭 타일을 추가(Flame 아이콘). 스트릭 수치는 `actionEvents`에서
  실시간으로 재계산하지 않고 **Progress 화면(P1)이 이미 쓰고 있는 `PROGRESS_SUMMARY.currentStreakDays`
  를 그대로 재사용**했다 — `completedAt`이 데모를 실행하는 실제 접속 시각 기준인 반면 앱의 내러티브
  "오늘"은 고정값(2026-08-30)이라, 실제 날짜로 스트릭을 다시 계산하면 두 값이 어긋나거나 접속할 때마다
  스트릭이 끊겨 보이는 문제가 생긴다 — Exec Dashboard(5차)에서 페르소나 화면 간 숫자를 일치시켰던 것과
  같은 원칙. 대신 "이번 주 N회" 카운터는 그대로 실시간 반응이라, 방금 완료한 항목은 바로 반영된다.
- **④ 개인 알람/리마인더** (신규 타입 `Reminder`, `src/types/index.ts` / 신규 시드 `src/data/mvpData.ts`
  `INITIAL_REMINDERS` / `src/lib/store.tsx` 상태·액션 / `src/components/sheets/ReminderComposer.tsx`
  신규 / `src/components/ui.tsx`에 재사용 가능한 `Toggle` 추가 / My Actions에 "리마인더" 카드 신규):
  두 종류로 나눠 구현했다 — **근무 시작 알림**(15/30/60분 전 오프셋 선택 + 켬/끔)은 설정 자체는 실제로
  저장되고 나중에 실제 백엔드가 붙으면 `offsetMinutes` 그대로 진짜 예약 푸시에 쓸 수 있지만, 데모의
  내러티브 "오늘"과 실제 접속 시각이 달라 실시간 카운트다운을 정직하게 보여줄 방법이 없어 각 리마인더
  행에 "지금 테스트"(🔔) 버튼을 둬서 즉시 확인할 수 있게 했다. **커스텀 리마인더**(사용자가 직접
  "몇 시에 뭘 알려줘"를 입력)는 실제 현재 시각과 비교해 **진짜로 동작한다** — 탭이 열려 있는 동안
  20초 간격으로 체크해서 시각이 일치하면 토스트 + (브라우저 알림 권한을 허용한 경우) 실제
  `Notification` API까지 띄운다. 리마인더를 켜거나 새로 추가하는 시점에 알림 권한을 요청.
- `tsc --noEmit` 클린, `npm run build` 성공. Playwright로 (1) 솔로 상태(팀 미참여)에서 "근무 일정"
  시트가 본인 행만 보여주고 셀프 입력이 실제로 저장되는지, (2) 팀 참여 후 같은 시트가 전체 팀 리스트로
  전환되고 본인 행 편집 + 교대 요청이 기존대로 동작하는지, (3) My Actions의 스트릭/주간 타일, 근무 시작
  알림 오프셋 전환, 커스텀 리마인더 추가·"지금 테스트" 토스트까지 전체 플로우 스크린샷 검증 후 Artifact
  재배포, 소스 재압축 전달.

## 16차: 근무 교대 요청 승인 주체를 매니저 → 상대 팀원으로 전환 (사용자 확인: "근무교대 요청하는 방식을, 팀원이 팀원한테 승인 알람을 보내서 상대방이 승인을 해주면 매니저한테 알람이 가는식으로 바꿔줘. 매니저가 승인하는거 말고.")

**배경**: 12차에서 구현한 근무 교대 요청은 "직원이 요청 → 매니저가 승인/거절"하는 구조였다. 사용자가
이를 명확히 뒤집어달라고 요청 — 매니저는 승인 권한을 갖지 않고, **요청을 받은 상대 팀원이 직접
승인/거절**하며, 승인되는 즉시 근무가 실제로 교환되고 매니저에게는 그 결과를 알리는 알림만 간다.
현장 실제 관행("동료끼리 먼저 맞추고 매니저는 통보만 받는다")에 더 가깝고, 매니저의 승인 대기 병목도
없앤다.

- **데이터 모델** (`src/types/index.ts`): `SwapRequest`에 `respondedAt`(상대가 승인/거절한 시각),
  `managerNotifiedAt`(승인되는 순간 매니저에게 알림이 간 시각 — 승인 권한이 아니라 FYI 기록) 필드
  추가. `status: 'pending' | 'approved' | 'rejected'` 자체는 그대로 두되, 의미가 "매니저의 결정
  대기"에서 "상대 팀원의 결정 대기"로 바뀐 것이 이번 개편의 핵심.
- **승인/거절 로직 이동** (`src/lib/store.tsx`): `approveSwap`/`rejectSwap`은 기존과 동일하게 로스터를
  실제로 맞바꾸지만(교환 로직 자체는 무변경), **호출 주체가 매니저 화면에서 직원(상대 팀원) 화면으로
  이동**했다. 승인 시 `managerNotifiedAt`을 함께 기록하고 토스트 문구도 "매니저 승인을 기다려주세요" →
  "OO님에게 승인 알람을 보냈어요"(요청 시), "OO님과 근무를 맞바꿨어요 — 매니저에게 알림을 보냈어요"
  (승인 시)로 교체.
- **단일 페르소나 제약 해결** — 로그인 페르소나가 지은 하나뿐이라 "상대 팀원이 승인하는" 수신 쪽을
  보여줄 화면이 없었다. `src/data/roster.ts`에 `INITIAL_SWAP_REQUESTS` 시드를 추가해, 박준서가 지은에게
  보낸 교대 요청(8/31 ↔ 9/1)이 앱 시작부터 대기 중인 상태로 존재하게 했다 — 이렇게 하면 지은 계정
  하나로 "요청을 보내는 쪽"(기존 SwapForm 플로우)과 "요청을 받아 승인하는 쪽"(신규) 양쪽을 모두 실제로
  시연할 수 있다.
- **직원 쪽 UI** (`src/components/sheets/TeamScheduleView.tsx`): "근무 일정" 시트에 "받은 교대 요청"
  섹션을 신규 추가(팀 참여 상태일 때만 노출) — 요청자·날짜·메모를 보여주고 거절/승인하기 버튼 제공,
  승인 시 `approveSwap` 호출로 즉시 로스터 반영. 기존 "내가 보낸 교대 요청" 섹션과 `SwapForm`의 안내
  문구도 "매니저가 승인하면" → "OO님이 승인하면 즉시 교대되고, 매니저에게도 알림이 가요"로 수정.
- **매니저 쪽 UI** (`src/manager/RosterView.tsx`): 기존 `SwapRequestsCard`의 승인(체크)/거절(X) 버튼을
  완전히 제거하고, **읽기 전용 알림 카드("근무 교대 알림")**로 재설계 — 대기 중인 요청은 "상대방 승인
  대기 중"(amber), 승인된 요청은 "교대 완료 · 자동 반영됨"(emerald) 배지만 보여준다. 매니저가 클릭해서
  결정할 수 있는 요소는 이 카드에 더 이상 없다.
- `tsc --noEmit` 클린, `npm run build` 성공. Playwright로 전체 플로우 검증: 팀 참여 → "근무 일정"
  시트에 박준서의 대기 중 요청이 실제로 뜨는지 확인 → 승인 전 지은의 8/31이 휴무인지 스크린샷 →
  "승인하기" 클릭 → 지은의 8/31이 박준서의 기존 근무(10:00–18:00)로 바뀌고 박준서는 휴무로 바뀐 것을
  확인(양방향 교환 정상) → Manager Dashboard "근무 일정 관리"로 전환해 같은 요청이 "교대 완료 · 자동
  반영됨" 배지의 읽기 전용 알림으로만 보이고, 화면 전체에 승인 버튼이 0개인 것까지 확인 후 Artifact
  재배포, 소스 재압축 전달.

## 17차: My Actions 완료 체크 되돌리기 (사용자 확인: "My action 탭에서 내가 액션 수행해서 체크하면 완료됐다고 아래로 이동되자나, 근데 실수로 누르거나 이런거 예방하기위해 눌렀다가 다시 언틱하면 다시 위 리스트로 올리게 할 수 있어?")

**배경**: My Actions에서 체크 버튼을 누르면 즉시 완료 처리되어 "완료됨" 섹션으로 내려가는데, 되돌릴
방법이 없어 실수로 탭했을 때 되돌릴 수 없다는 점을 사용자가 지적했다. 완료된 체크 버튼을 다시 탭하면
완료를 취소하고 다시 진행 중 목록으로 되돌리는 기능을 추가했다.

- **`src/lib/store.tsx`**: `uncompleteAction(id)` 신규 액션 추가 — 해당 액션의 `progress`를 1 되돌리고
  `completedAt`을 지운다(완료 시 `completeAction`이 했던 것의 정확한 역연산). 완료 시 `actionEvents`에
  남겼던 실적 로그(§5-2 — 나중에 실제 성과 그래프를 만들 때 쓰일 조용한 이벤트 로그)도 해당 액션의
  가장 최근 항목을 함께 제거해, "취소된 완료"가 향후 통계에 섞이지 않게 했다. `weeklyCompletionCount`는
  `actions` 배열의 `completedAt` 존재 여부로 매번 다시 계산되는 파생값이라 별도 처리 없이 자동으로
  반영된다.
- **`src/screens/MyActions.tsx`**: `ActionRow`의 체크 버튼에서 `disabled={done}`을 제거하고, 완료
  상태일 때 탭하면 `completeAction` 대신 `uncompleteAction`을 호출하도록 변경. 버튼에 `title="탭하면
  완료를 취소해요"` 툴팁과 `완료 취소하기` aria-label을 추가해 되돌릴 수 있다는 걸 명확히 했다.
- `tsc --noEmit` 클린, `npm run build` 성공. Playwright로 진행 중 항목 하나를 완료 → "완료됨"으로
  이동하고 "이번 주" 카운트가 1 증가하는 것 확인 → 방금 완료한 항목의 체크 버튼을 다시 탭 → 다시
  "진행 중" 목록으로 돌아오고 카운트도 원래대로 줄어드는 것까지 확인 후 Artifact 재배포, 소스 재압축
  전달.

## 18차: 동료 그룹(Crew) 생성/참여 — 매니저 없이도 직원끼리 팀 꾸리기 (사용자 확인: "그럼 좋아. 너가 제안한거에 나도 동의해. 동료그룹 방향성 유지하고 서브팀 기능은 현재로서는 무시해줘. 이제 아티팩스에 반영해줘." — 앞서 전략 문서 §9로 정리한 제안에 대한 승인)

**배경**: 사용자가 "팀을 팀원 개인이 꾸릴 수도 있나? 매니저 사용 전에 동료들과 함께 팀을 꾸리는거야. 그리고
팀별로도 나눠서 관리하는 기능은?"이라고 물어, CPO 관점에서 두 개념(매니저 없이 직원끼리 먼저 모이는
바텀업 팀 생성 / 매장을 홀서빙·카운터·재고관리·영업처럼 기능별로 나누는 조직 구조)을 분리해 각각의
가치와 비용을 짚어 전략 문서 §9로 정리·제안(직원 주도 "동료 그룹"은 Free 티어로 채택 추천, 기능별
"부서" 분리는 로스터·Will×Capability·체크리스트·Executive 롤업을 전부 건드리는 스코프 폭발이라 Business
티어 로드맵으로 보류 추천) → 사용자가 그대로 승인, 동료 그룹만 지금 구현하기로 확정.

- **핵심 설계 판단 — Team 티어 결제 트리거를 잠식하지 않는 스코프 경계**: 기존 `hasJoinedTeam`(boolean)이
  "매니저가 매장을 개설해야만 팀 기능이 열린다"는 전제로 짜여 있었는데, 이걸 그대로 두고 직원이 무료로
  팀을 통째로 만들 수 있게 하면 나중에 매니저가 들어왔을 때 "이미 다 되는데 왜 결제하지?"가 될 위험이
  있다(§9-1). 그래서 boolean을 `TeamMembership = 'none' | 'crew' | 'store'` 3단계로 승격하고,
  **'crew'(동료 그룹)는 일정 공유 + 근무 교대 요청까지만**, 공지·인수인계·매니저 대시보드가 걸린
  **'store'(매니저 매장 참여)는 기존과 동일하게 전부** 열리도록 스코프를 엄격히 나눴다
  (`src/types/index.ts`).
- **`src/lib/store.tsx`**: `createCrew()` 신규 — 직원이 코드를 즉시 발급받아(`CREW-XXXX` 형식) 스스로
  `'crew'`로 참여한다. `joinTeam(code)`는 기존 매장 코드(`STORE_CODE`)와 동료 그룹 코드를 같은 입력창
  에서 함께 받아 자동으로 구분해 `'store'`/`'crew'`로 갈라 참여시키도록 확장 — 단일 페르소나(지은) 제약상
  "다른 동료가 이미 만들어 둔 그룹에 참여하는" 쪽도 시연할 수 있도록 `src/data/mvpData.ts`에
  `CREW_DEMO_CODE = 'CREW-9F2Q'`(박준서가 미리 만들어 뒀다는 설정) 데모 코드를 심었다 — 새로 만들기와
  코드로 참여하기 양쪽 흐름을 모두 지은 한 계정으로 확인 가능.
- **`src/components/sheets/TeamScheduleView.tsx`**: 미참여(`'none'`) 상태의 안내 카드를 "코드로
  참여하기"(기존 매장 참여) / "동료 그룹 만들기"(신규) 두 버튼으로 확장. `'crew'`로 참여 중이면 상단에
  발급된 코드 + 복사 버튼 + "공지·인수인계는 매니저가 매장을 개설하면 열려요" 설명이 달린 카드를 노출해
  언제든 동료에게 코드를 공유할 수 있게 했다. 팀 전체 일정 보기·셀프 편집·근무 교대 요청·받은 교대 요청
  승인/거절은 `membership !== 'none'`이면 모두 그대로 동작(즉 동료 그룹도 14~16차 로스터/스왑
  아키텍처를 그대로 재사용 — 별도 구현 없이 스코프에 포함됨).
  (※ 19차에서 코드 카드와 미참여 안내는 Team 탭으로 옮겨짐. 아래 참고)
- **`src/screens/TeamFeed.tsx`(Team 탭)**: `membership`이 `'store'`일 때만 기존처럼 공지+인수인계
  피드가 보인다. `'crew'`일 때는 "동료 그룹에 참여 중이에요 · 공지·인수인계는 매니저가 매장을 개설하면
  열려요"라는 별도 안내(+ "매장 코드로 참여하기" 버튼)를 보여줘, 그냥 "팀이 없다"고 오해하지 않고 지금
  뭘 할 수 있고 뭘 못 하는지 정직하게 알 수 있게 했다. `'none'`일 때는 기존 안내 그대로.
- **`src/auth/JoinTeamForm.tsx`**: 입력창 안내 문구·placeholder를 매장 코드/동료 그룹 코드 둘 다
  받는다는 걸 알 수 있도록 수정("예: GN-4821 또는 CREW-9F2Q").
- **아직 미해결로 남겨둔 것**: 나중에 진짜 매니저가 매장을 개설했을 때 이미 있는 동료 그룹을 매장으로
  "승격/흡수"하는 흐름은 이번 라운드에서 설계하지 않았다 — 지금은 두 엔티티가 완전히 독립적이라, 같은
  매장 사람들이 동료 그룹과 매장에 각각 따로 참여한 채로 남을 수 있다. 전략 문서 §8/§9-1에 다음
  우선순위 항목으로 기록.
- `tsc --noEmit` 클린, `npm run build` 성공. Playwright로 (1) 솔로 상태에서 "동료 그룹 만들기" 클릭 →
  즉시 팀 전체 일정 보기로 전환되고 발급된 코드 카드가 뜨는지, 받은 교대 요청(박준서 시드)까지 함께
  보이는지, (2) Team 탭이 "동료 그룹에 참여 중이에요" 안내로 바뀌는지, (3) 별도 세션에서 `CREW-9F2Q`
  코드로 참여했을 때도 동일하게 팀 전체 일정·코드 카드가 뜨는지까지 스크린샷 검증 후 Artifact 재배포,
  소스 재압축 전달. 전략 문서(`v2-strategy-b2c-pivot.md`) §9-1도 "채택·구현 완료(18차)"로 갱신.

## 19차: 동료그룹 코드 Team 탭 이동 + 공지 작성 권한 전체 확대 + 로그인 온보딩 Team 탭 통합 (사용자 확인: "동료그룹코드를 팀탭으로 옮겨주고, 공지도 매니저만 남기는게 아니고 다들 권한이 있게 해줘. 매니저 공지는 아이콘이나 색깔을 다르게해서 관리자 공지라는 차별점을 주고. 그리고 처음 계정 로그인 하고, 매장 또는 그룹 코드 넣는 화면을 팀탭에 넣어줘.")

**배경**: 18차에서 구현한 동료 그룹(Crew) 기능이 근무 일정 시트 안에 코드 카드로 들어가 있었고, 공지는
여전히 매니저 전용이었으며, 로그인 직후 매장/그룹 코드를 입력하는 화면이 앱 진입 전 별도 풀스크린
단계로 분리되어 있었다. 사용자가 이 세 가지를 한 번에 정리해달라고 요청 — 요약하면 "팀 관련 기능은
Team 탭 하나로 모으고, 공지는 팀원 모두가 남길 수 있되 매니저 공지는 시각적으로 구분되게" 해달라는
것.

- **① 동료 그룹 코드 카드를 근무 일정 시트 → Team 탭으로 이동** (`src/components/sheets/
  TeamScheduleView.tsx` → `src/screens/TeamFeed.tsx`): 코드 표시 + 복사 버튼 UI를 그대로 옮기되,
  `CrewFeedPrompt`(동료 그룹 상태에서 보여주던 "공지·인수인계는 매니저가 매장을 개설하면 열려요" 안내)
  상단에 합쳐 넣었다. 근무 일정 시트의 미참여(`'none'`) 상태 카드는 "코드로 참여하기"/"동료 그룹
  만들기" 두 버튼 대신 **"Team 탭에서 참여하기"** 버튼 하나로 단순화 — 누르면 시트를 닫고 바로 Team
  탭으로 이동한다(`SheetHost.tsx`에 `onGoToTeam` 콜백을 새로 배선, `shiftDetail`의 "오늘 할 일 보기"
  →`onNavigate('myActions')` 패턴과 동일한 방식).
- **② 공지 작성 권한을 팀원 전체로 확대** (`src/types/index.ts`, `src/lib/store.tsx`): `Announcement.
  authorRole`을 `'manager' | 'system'`에서 `'manager' | 'employee' | 'system'`으로 확장. 기존
  `addAnnouncement`(매니저 전용, 상단 고정 가능 — `TeamActionsComposer.tsx`가 그대로 사용)는 건드리지
  않고, `addTeamPost(message)` 신규 액션을 추가해 `authorRole: 'employee'`, `pinned: false`로 공지를
  남긴다 — **고정(pinned) 권한은 여전히 매니저만** 갖는다는 원칙은 유지(고정은 "이 공지가 우선순위가
  높다"는 권위적 판단이라 전체 개방 대상이 아니라고 판단). 신규 `AnnouncementComposer.tsx`(
  `HandoverComposer.tsx`와 동일한 패턴, 상단 고정 체크박스 없음)를 만들어 `SheetKind`에
  `announcementCompose`로 배선. Team 탭 헤더에 공지 작성(확성기 아이콘) 버튼을 인수인계 추가(＋)
  버튼 옆에 새로 배치.
- **③ 매니저 공지 시각적 차별화** (`src/screens/TeamFeed.tsx`의 `AnnouncementCard`): `authorRole ===
  'manager'`면 아바타를 앰버 톤 배경 + 확성기(Megaphone) 아이콘으로, 작성자명 옆에 `Badge tone="amber"`
  로 "관리자 공지" 배지를 붙이고 하단 메타 텍스트도 "매니저 · N분 전"으로 표시. 그 외(`'employee'`)는
  기존 브랜드색 아바타 + 이니셜을 그대로 쓰고 배지 없이 "팀원 · N분 전"으로 표시 — 위계적으로 튀지
  않으면서도 "이건 관리자가 남긴 공지"라는 신호는 명확하게 구분.
- **④ 로그인 직후 온보딩 화면을 Team 탭 안으로 통합** (`App.tsx`, `src/screens/TeamFeed.tsx`): 9차에서
  만든 `Stage = 'login' | 'join' | 'app'` 3단계 상태 머신에서 `'join'` 스테이지를 완전히 제거
  (`Stage = 'login' | 'app'`) — 로그인하면 곧장 앱으로 들어간다. `JoinTeamScreen.tsx`가 보여주던
  문구("근무하는 매장에 참여해보세요" + 코드 입력 안내)와 `JoinTeamForm`을 그대로 Team 탭의
  `OnboardingJoinSection`으로 옮기고, 구분선 아래 "동료 그룹 만들기" 버튼을 나란히 배치해 코드가 없는
  사람도 그 자리에서 바로 스스로 그룹을 시작할 수 있게 했다. `JoinTeamScreen.tsx` 파일 자체는 삭제하지
  않고 unimported 상태로 남김("hide, don't delete" 원칙).
- **설계 판단 — 전략 문서 §9-1의 크루/매장 경계는 건드리지 않음**: "다들 권한이 있게 해줘"라는 요청을
  "공지를 볼 수 있는 사람 전체로 피드 자체를 연다"가 아니라 **"이미 피드가 보이는 컨텍스트(매장·`'store'`
  멤버십) 안에서, 작성자를 매니저로 한정하지 않는다"**로 해석해 구현했다 — 즉 동료 그룹(`'crew'`)
  멤버십은 여전히 공지·인수인계 피드 자체가 열리지 않는다(18차에서 정한 Team 티어 결제 트리거 보존
  원칙 그대로). 이 해석은 사용자에게 별도로 확인받지 않고 진행한 판단이라 여기 명시해 둔다 — 만약
  "동료 그룹도 공지를 보고 남길 수 있어야 한다"는 의도였다면 §9-1의 티어 경계 자체를 다시 논의해야
  한다.
- `tsc --noEmit` 클린, `npm run build` 성공. Playwright로 (1) 로그인 직후 별도 화면 없이 바로 앱에
  진입하고 Team 탭에 코드 입력 폼 + 동료 그룹 만들기 버튼이 인라인으로 보이는지, (2) 근무 일정 시트의
  미참여 카드에 동료 그룹 코드 카드가 더 이상 없고 "Team 탭에서 참여하기" 버튼이 정상적으로 Team 탭으로
  이동시키는지, (3) 매장 코드(`GN-4821`)로 참여한 뒤 공지 작성 버튼이 보이고 직접 남긴 공지가 매니저
  아바타/배지 없이 팀원 스타일로 뜨는지, (4) Manager Dashboard에서 남긴 공지가 같은 피드에서 앰버 아바타
  + "관리자 공지" 배지로 뜨는지, (5) "동료 그룹 만들기"로 크루를 만들면 Team 탭에 코드 카드 + 크루 안내가
  함께 뜨는지까지 스크린샷으로 확인 후 Artifact 재배포, 소스 재압축 전달.

## 20차: 매니저 PRO 참여 코드 커스터마이즈 + 솔로 UX 플로우 리뷰 (사용자 확인: "매니저 프로기능으로 참여코드 커스터마이즈 기능 만들어줘. 그리고 UX 관점에서 개인이 사용하기에 플로우가 어떤지 전체 리뷰해주고, 생략 또는 추가하면 좋을 기능들에대한 아이디어도 줘봐. 현재 기능에 대한 피드백도 좋아.")

두 개의 서로 다른 요청 — (1) 매니저 PRO 신기능 구현, (2) 솔로 사용자 관점의 전체 UX 플로우 리뷰 —
가 함께 왔다. (1)은 코드로 구현하고, (2)는 대화로 CPO 관점 피드백을 전달했다.

- **매니저 PRO — 참여 코드 커스터마이즈** (`src/data/mvpData.ts`, `src/lib/store.tsx`,
  `src/manager/TeamInvite.tsx`): 9차부터 고정 상수였던 `STORE_CODE = 'GN-4821'`을 `AppStateProvider`의
  공유 상태(`storeCode`)로 승격 — `joinTeam()`도 이제 이 동적 값을 기준으로 비교한다. 매니저 쪽에
  "코드 커스터마이즈" PRO 카드(`CodeCustomizer`, Crown 배지)를 신규 추가 — 연필 아이콘으로 편집 모드에
  들어가면 직접 원하는 코드를 입력하거나(예: `GANGNAM2026`) 주사위 아이콘으로 무작위 재발급도 할 수
  있다. **검증 로직**: 3~20자, 영문 대문자·숫자·하이픈만 허용, `CREW-`로 시작하는 코드는 거부(그
  접두사는 `createCrew()`가 발급하는 동료 그룹 코드 전용으로 예약되어 있어 두 코드 종류가 섞이는 걸
  방지). 코드를 바꾸면 그 순간부터 **이전 코드는 완전히 무효화**된다(신구 코드 동시 허용 없음) —
  이미 참여한 직원의 membership 상태에는 영향 없음. `STORE_JOIN_LINK`도 `buildStoreJoinLink(code)`
  헬퍼로 동적 계산되도록 함께 바꿔, 참여 링크가 항상 최신 코드를 반영하도록 함.
- `tsc --noEmit` 클린, `npm run build` 성공. Playwright로 (1) 3자 미만 코드 입력 시 에러, (2) `CREW-`
  접두사 입력 시 전용 예약 에러, (3) 유효한 커스텀 코드(`GANGNAM2026`) 저장 성공, (4) Employee App에서
  **이전 코드(GN-4821)로 참여 시도 시 실패**, **새 코드(GANGNAM2026)로는 즉시 참여 성공**까지 스크린샷
  검증 후 Artifact 재배포, 소스 재압축 전달.
- **솔로 UX 플로우 리뷰(대화로 전달)**: 로그인→Team 탭 온보딩→My Shift→My Actions 전체 흐름을
  솔로(미참여) 사용자 관점에서 훑어 마찰 지점 5건(① 로그인 직후 MoodCheckIn이 곧바로 뜨는 타이밍,
  ② My Shift 인수인계 섹션이 `membership`과 무관하게 항상 열려 있어 Team 탭의 "아직 미참여" 안내와
  모순되는 점, ③ Team 탭 온보딩에서 코드 입력 폼이 "동료 그룹 만들기"보다 먼저 보이는 순서, ④ 동료
  그룹을 막 만든 직후 공유 동선이 없는 점, ⑤ My Actions의 시각적 위계가 평평해 "오늘 할 일"이 묻히는
  점)과 기능 추가 아이디어 4가지(예상 급여 계산기, 월간 캘린더 뷰, 반복 근무 등록, 완료 히스토리/주간
  트렌드)를 정리해 전달. 이번 라운드에서는 전략 문서나 코드에 반영하지 않고 피드백만 전달 — **다음
  라운드(21차)에서 사용자가 전체 채택을 확정하고 바로 구현.**

## 21차: 솔로 UX 리뷰 피드백 전면 반영 — 마찰 지점 5건 수정 + 예상 급여 계산기·월간 캘린더·반복 근무 등록·습관 그래프 (사용자 확인: "좋아. 너가 준 마찰지점 등의 피드백을 모두 반영하고, 추가하면 좋은 아이디어 1~4도 다 반영해줘. 단, 1번 급여 계산 같은 경우, 공휴수당이나 추가수당이 반영되게 해야하는데, 이거는 더 복잡해질수가 있어서 정확한 급여라기보다 예상 급여, 그리고 공휴수당이나 추가 수당 시간을 본인이 기입 할 수 있게 해주는게 좋을거같아.")

20차에서 대화로만 전달했던 솔로 UX 리뷰 전체(마찰 지점 5건 + 기능 아이디어 4건)를 한 번에 채택·구현.
예상 급여 계산기는 사용자가 직접 스코프를 좁혀줬다 — "정확한 급여"를 표방하지 않고, 공휴수당·연장수당처럼
계산이 복잡해지는 항목은 자동 산출 대신 사용자가 시간을 직접 입력하는 방식으로.

**마찰 지점 5건 수정**

- **① 컨디션 체크인 타이밍 재검토** (`src/lib/store.tsx`, `src/components/MoodCheckIn.tsx`): 로그인
  직후 즉시 뜨던 걸 없애고, `moodPromptOpen`이라는 별도 상태로 실제 노출 여부를 관리한다. "오늘 근무
  상세"(`shiftDetail` 시트, 오늘 시프트)를 열려고 할 때 아직 체크인 전이면 `openSheet()`가 그 요청을
  `pendingSheetAfterMood`에 잠깐 보류하고 먼저 체크인 모달을 띄운 뒤, 체크인을 마치거나 스킵하면
  보류해둔 시트를 이어서 연다 — 앱을 한 번도 못 본 상태에서 컨디션부터 묻던 것을, 하루를 실제로
  시작하는 자연스러운 지점(오늘 근무를 열어보는 순간)으로 옮겼다.
- **② My Shift ↔ Team 탭 membership 불일치 해소** (`src/screens/MyShift.tsx`): "최근 인수인계" 섹션과
  "인수인계 남기기" 버튼이 `membership`과 무관하게 항상 열려 있던 걸, `membership === 'store'`일 때만
  열리도록 게이팅했다. 그 외에는 잠금 아이콘 + "인수인계는 매장 팀에 참여하면 시작돼요" + "Team 탭에서
  참여하기" 버튼(신규 `onNavigate` prop으로 Team 탭 이동, `App.tsx`에서 `MyShift`에 배선)을 보여줘
  Team 탭의 온보딩 상태와 같은 신호를 준다.
- **③ Team 탭 온보딩 순서 반전** (`src/screens/TeamFeed.tsx`): `OnboardingJoinSection`에서 "동료 그룹
  만들기"를 1차 CTA(PrimaryButton, 흰 배경)로 올리고, 코드 입력 폼은 "코드가 있다면" 구분선 아래
  보조 옵션으로 내렸다 — 정말 처음 앱을 깐 사람 대부분은 코드가 없는 쪽이 흔하다는 전제를 반영.
- **④ 동료 그룹 생성 직후 공유 동선 추가** (`src/screens/TeamFeed.tsx`): `CrewFeedPrompt`의 코드 카드에
  "동료에게 지금 공유하기" 버튼(PrimaryButton)을 추가 — `navigator.share`(Web Share API)가 있으면
  기기의 공유 시트를 그대로 띄우고, 없거나 취소되면 기존 클립보드 복사로 자동 대체한다.
- **⑤ My Actions 시각 위계 정리** (`src/screens/MyActions.tsx`): "오늘 할 일" 액션 리스트에
  `SectionLabel`을 붙이고 리마인더 섹션보다 위로 올렸다 — 스트릭/카운트 타일 → (신규) 습관 그래프 →
  **오늘 할 일** → AI 추천 배너 → 리마인더 → 완료됨 순서로, 제일 중요한 "지금 할 일"이 다른 카드들에
  묻히지 않게 했다.

**기능 아이디어 4건 반영**

- **① 예상 급여 계산기** (`src/types/index.ts`의 `WageSettings`/`ExtraPayEntry`, `src/lib/wageCalc.ts`
  신규 순수 함수 모듈, `src/lib/store.tsx`, `src/components/sheets/WageCalculatorView.tsx` 신규,
  `src/screens/MyShift.tsx`의 `WagePreviewCard`): 사용자가 명시한 대로 **"정확한 급여"가 아니라
  추정치**라는 전제를 화면 문구에 계속 노출한다("세금·공제 전이고 실제 급여와 다를 수 있어요"). 시급은
  사용자가 직접 입력(기본값 0 — 실제 최저임금 수치를 임의로 단정하지 않기 위해 미설정 상태로 시작해
  첫 사용 시 입력을 유도), 완료·진행 중 근무와 예정된 근무의 예상액을 각각 시간×시급으로 계산해
  구분 표시. **공휴수당·연장수당은 자동 계산하지 않고**, 사용자가 항목명("추석 연휴 근무" 등)과 시간을
  직접 입력하면 `overtimeMultiplier`(기본 1.5배)를 곱해 단순 가산 — 계산이 복잡해지는 걸 피하면서도
  실제로 있는 수당을 반영할 수 있게 한 절충. My Shift 히어로 카드 바로 아래(매 근무 후 확인할 만한
  가장 눈에 띄는 위치)에 총액 미리보기 카드를 두고 탭하면 상세 계산 시트로 들어간다.
  (※ 22차에서 이 진입점이 My Actions 화면 하단으로 옮겨짐. 아래 참고) Playwright로 시급 10,000원
  저장 → 완료 40시간(400,000원)+예정 16시간(160,000원) 합산 정확히 계산되는 것과, 추가 수당 8시간
  기록 시 120,000원(8×10,000×1.5)이 정확히 더해져 총액이 갱신되는 것까지 검증.
- **② 월간 캘린더 뷰** (`src/components/MonthCalendar.tsx` 신규): My Shift "예정된 근무" 헤더에
  목록/캘린더 토글 버튼을 추가 — 캘린더 모드에서는 리스트 대신 표준 월 그리드가 나오고, 날짜 셀에
  진행중(브랜드색)/예정(앰버)/완료(회색) 점으로 상태를 표시하며 탭하면 해당 날짜의 시프트 상세 시트가
  열린다. `mockData.ts`의 `shifts`가 실제로 걸쳐 있는 달만 좌우 화살표로 넘나들 수 있게 해, 데이터가
  없는 달을 만들어내지 않는다.
- **③ 반복 근무 등록** (`src/manager/RosterView.tsx`의 `EditShiftModal`, `src/components/sheets/
  TeamScheduleView.tsx`의 `mirrorWeekDate()`): `ROSTER_WEEKS`가 "이번 주"/"다음 주" 딱 2주치만 갖고
  있다는 현재 데이터 모델의 한계를 정직하게 반영해, "반복"을 무한 규칙이 아니라 **지금 편집 중인
  날짜와 같은 요일이 다른 한 주에도 있으면 그 날짜를 찾아 한 번에 같이 적용하는 것**으로 스코프를
  좁혔다 — 셀프 근무 입력(15차)에서 매주 같은 요일을 두 번 따로 입력해야 했던 마찰을 줄인다. 편집
  모달에 "9.6(일)에도 같은 근무로 반복 적용" 같은 체크박스가 나타나고(셀프 근무 입력에서만 노출 —
  매니저의 팀 전체 로스터 편집에는 영향 없음), 체크 후 저장하면 두 날짜 모두 `updateRosterEntry`로
  한 번에 갱신된다. Playwright로 8.30(일) 종료 시각을 23:00로 바꾸고 반복 체크 → 다음 주 9.6(일)도
  동일하게 14:00–23:00로 반영되는 것까지 확인.
- **④ 완료 히스토리/주간 트렌드** (`src/data/mvpData.ts`의 `ACTION_TREND_HISTORY`, `src/lib/store.tsx`의
  `weeklyActionTrend`, `src/screens/MyActions.tsx`의 `WeeklyTrendMini`): "이번 주 N회"만으로는 지난
  주 대비 흐름이 안 보인다는 지적을 반영한 작은 막대 그래프. 지난 3주는 목업 값이고 마지막 막대(이번
  주)만 실시간 `weeklyCompletionCount`로 채워 방금 완료한 항목이 바로 반영된다. **XP/레벨 숨김 원칙은
  그대로 유지** — "성장 그래프"가 아니라 스트릭이 왜 끊겼는지 돌아볼 수 있는 정도의 "습관 그래프" 톤만
  최소한으로 보여준다. (※ 22차에서 사용자 피드백에 따라 이 배너 자체가 제거됨. 아래 참고)

- `tsc --noEmit` 클린, `npm run build` 성공(886KB, gzip 247KB). Playwright로 전체 플로우 — 로그인 직후
  MoodCheckIn 미노출 → 오늘 근무 카드 탭 시 노출 및 체크인 후 원래 시트로 자동 진행 → 예상 급여 카드/
  시트(시급 저장·추가 수당 기록·총액 계산) → 인수인계 섹션 잠금(미참여 상태) → 캘린더 뷰 토글 →
  Team 탭 온보딩 순서(동료 그룹 만들기 우선) → 동료 그룹 생성 후 공유 버튼 → My Actions 순서(습관
  그래프 → 오늘 할 일 → 리마인더) → 반복 근무 등록(다음 주 미러 날짜 반영)까지 스크린샷으로 검증 후
  Artifact 재배포, 소스 재압축 전달.

## 22차: 급여계산기 My Actions 이동 + 하단 탭 재배치 + Team 공지/인수인계 확인·히스토리 + My Shift 인수인계 배너 접기 + 온보딩 랜딩 화면 (사용자 확인: "1. 급여계산 탭은 My Action 탭안으로 넣고, 화면에서 하단으로 배치해줘. 2. My Action 탭 안에 4주 추의 배너랑 AI생성 기능 배너는 아에 없애줘. 3. My Action 탭을 My Shift 다음으로, 전체 화면 하단 중앙에 배치해줘. 4. Team 탭의 공지와 인수인계 내용은 접어두기 또는 확인 기능을 통해 확인한 내용은 계속 뜨지 않고 히스토리로만 남게 해줘. 5. My shift 탭안에 최근 인수인계 배너도 접었다 폈다 할 수 있게 해줘. 6. 온보딩 랜딩 화면 만들어줘.")

6개 요청 전부 UI/네비게이션 구조 조정 + 신규 화면 1개. 순서대로 정리.

- **① 예상 급여 계산기를 My Shift → My Actions로 이동, 화면 맨 아래 배치** (`src/screens/MyShift.tsx`,
  `src/screens/MyActions.tsx`): 21차에서 My Shift 히어로 카드 바로 아래 있던 `WagePreviewCard`
  컴포넌트를 그대로 잘라 My Actions로 옮기고, "완료됨" 섹션 다음(화면에서 스크롤했을 때 가장 마지막)에
  `SectionLabel`("예상 급여")과 함께 배치했다. 로직·문구는 21차 그대로(예상치라는 disclaimer, 시급
  미설정 시 설정 유도, 탭하면 `wageCalculator` 시트) — 위치만 옮겼다.
- **② My Actions에서 4주 완료 추이 배너 + AI 추천 배너 제거** (`src/screens/MyActions.tsx`): 21차에서
  추가했던 `WeeklyTrendMini`(습관 그래프) 컴포넌트와 그 렌더링을 완전히 삭제, 6차부터 있던
  "AI에게 부탁해보세요" 배너(Sparkles 아이콘 + `actionCompose` 시트 유도 버튼)도 함께 삭제했다.
  스토어의 `weeklyActionTrend` 파생값 자체는 그대로 남아 있지만(제거해도 다른 곳에서 안 쓰므로 무해),
  화면에는 더 이상 노출되지 않는다. 남은 순서: 스트릭/카운트 타일 → **오늘 할 일** → 리마인더 →
  완료됨 → (신규 위치) 예상 급여.
- **③ 하단 탭 순서를 My Shift → My Actions → Team으로, My Actions를 정중앙에 배치**
  (`src/components/BottomNav.tsx`): 기존 `[myShift, teamFeed, myActions]` 배열 순서를
  `[myShift, myActions, teamFeed]`로 변경 — 3탭 구조라 가운데 자리에 오는 것만으로 "My Shift 바로
  다음"과 "화면 하단 정중앙" 두 요구가 함께 충족된다.
- **④ Team 탭 공지/인수인계에 "확인" 기능 + 히스토리 섹션** (`src/lib/store.tsx`,
  `src/screens/TeamFeed.tsx`): 전역 상태에 `readFeedIds: string[]`와 `acknowledgeFeedItem(id)`를
  신규 추가했다 — 화면은 탭을 전환할 때마다 언마운트/재마운트되기 때문에(다른 화면들의 로컬 `useState`
  패턴과 동일하게 화면 안에 두면 Team 탭을 나갔다 오는 순간 확인 상태가 초기화됨) 반드시 전역
  `AppStateProvider`에 둬야 탭을 오가도 유지된다. `AnnouncementCard`/`HandoverCard`에 `onAck?` prop을
  추가해 각 카드 하단에 "확인했어요"(체크 아이콘) 버튼을 배치 — 누르면 `readFeedIds`에 추가되고 그
  즉시 메인 피드에서 사라진다. 메인 피드는 `readFeedIds`에 없는 항목만, 새로 만든 `HistorySection`
  컴포넌트는 있는 항목만 보여주는 접힌 디스클로저(기본 닫힘, "히스토리 N건" 클릭 시 펼침)로 렌더링 —
  히스토리에서는 같은 카드를 재사용하되 `onAck`를 넘기지 않아 "확인했어요" 버튼이 다시 뜨지 않는다.
  둘 다 비어 있고 히스토리만 있을 때는 "새로운 공지·인수인계가 없어요" 안내 문구를 보여준다.
- **⑤ My Shift 최근 인수인계 배너 전체 접기/펼치기** (`src/screens/MyShift.tsx`): 기존에 있던
  "전체 N건 보기"(카드 내부에서 더 보여줄지 결정하는 하위 토글)는 그대로 두고, 그 위에 배너 전체를
  여닫는 상위 토글을 새로 추가했다 — `SectionLabel` 옆에 chevron 버튼을 두고 `handoverBannerOpen`
  상태(기본값 펼침)로 카드 전체(잠금 상태의 안내 카드 포함)를 보이거나 숨긴다. 닫혀 있을 때도 몇 건
  있는지는 라벨 옆 작은 카운트로 바로 확인할 수 있게 했다.
- **⑥ 온보딩 랜딩 화면 신설** (`src/onboarding/OnboardingScreen.tsx` 신규, `App.tsx`): 로그인 버튼
  화면(`LoginScreen`) 바로 앞에 붙는 3장짜리 소개 카드 — My Shift(근무·예상 급여 정리) / My
  Actions(오늘 할 일·리마인더) / Team(공지·인수인계·근무 교대) 각 탭의 가치를 한 장씩 아이콘 + 짧은
  카피로 보여준다. 우상단 "건너뛰기"로 언제든 바로 로그인 화면으로 넘어갈 수 있고, 하단 점 인디케이터
  + "다음"/마지막 장에서는 "시작하기" 버튼으로 진행한다. `App.tsx`의 `Stage`를
  `'login' | 'app'`에서 `'onboarding' | 'login' | 'app'`으로 확장하고 기본값을 `'onboarding'`으로
  설정 — 단, 로그아웃(데모 재시작) 버튼은 온보딩을 건너뛰고 바로 `'login'`으로 보내, 온보딩은 정말
  "처음 앱을 여는 순간"에만 한 번 보이도록 했다.
- `tsc --noEmit` 클린, `npm run build` 성공(892KB, gzip 248KB). Playwright로 (1) 앱 진입 시 온보딩
  3장이 순서대로 넘어가고 "건너뛰기"/"시작하기" 모두 로그인 화면으로 이어지는지, (2) 로그인 후 하단
  탭 순서가 My Shift/My Actions/Team이고 My Actions가 정중앙인지, (3) My Shift에 예상 급여 카드가
  더 이상 없고 "최근 인수인계" 배너가 라벨 옆 chevron으로 접히고 펼쳐지는지, (4) My Actions에 습관
  그래프·AI 배너가 없고 화면 맨 아래(완료됨 다음)에 예상 급여 카드가 있으며 탭하면 시트가 정상적으로
  열리는지, (5) Team 탭에서 매장 코드로 참여한 뒤 공지/인수인계에 "확인했어요"를 누르면 즉시 메인
  피드에서 사라지고 "히스토리 N건" 디스클로저를 펼치면 확인 버튼 없이 그 항목이 다시 보이는지까지
  스크린샷 검증 후 Artifact 재배포, 소스 재압축 전달.

## 산출물
- 소스 코드: `shyftstarter-app.zip`으로 전달 — `npm install && npm run dev` (진입 시 **온보딩 3장
  소개 화면(22차)** → 로그인 데모 → 곧장 앱 진입(19차부터 별도 팀 참여 화면 없음) → 상단 전환 버튼으로
  Employee App/Manager Dashboard/Executive Dashboard 세 페르소나 모두 확인 가능. Employee App 하단
  탭 순서는 **My Shift → My Actions → Team(22차, My Actions가 정중앙)**. **My Shift**에서: 오늘 근무
  카드 → 최근 인수인계 배너(22차 — 라벨 옆 chevron으로 전체 접기/펼치기 가능, 인수인계는 매장 팀
  (`membership === 'store'`) 참여 시에만 내용이 열림 · 미참여 시 Team 탭으로 안내) → "예정된 근무"는
  목록/캘린더(21차, 월 그리드) 토글 → "근무 일정" 시트에서 본인 근무를 직접 입력/수정(반복 적용
  체크박스로 다른 한 주도 한 번에 — 21차)하거나 참여 상태면 팀 전체 일정 + 근무 교대(PRO) + 받은 교대
  요청 승인/거절(박준서 시드 요청). **My Actions**에서: 연속 스트릭/주간 완료 타일 → 오늘 할 일(완료
  체크는 다시 탭하면 취소) → 근무 시작 알림·커스텀 리마인더(실시간 작동) → 완료됨 → **예상 급여
  카드(22차, 화면 맨 아래 — 탭하면 시급 설정/추가 수당 기록/총액 상세)**. 4주 습관 그래프와 AI 추천
  배너는 22차에 제거됨. 첫 로그인 시 MoodCheckIn은 즉시 뜨지 않고 오늘 근무를 열 때 뜸(21차). **Team
  탭**에서: 미참여 상태면 "동료 그룹 만들기"가 1차 CTA로, 코드 입력은 "코드가 있다면" 보조 옵션으로
  (21차); 매장 코드나 동료 그룹 코드(`CREW-9F2Q`) 입력도 가능(매장 코드는 기본 `GN-4821`이지만 매니저가
  커스터마이즈했으면 그 값으로 바뀜 — 20차); 매장 참여 상태면 공지(팀원 누구나 작성 가능, 매니저 공지는
  앰버 배지로 구분)+인수인계 피드 — 각 카드의 "확인했어요"를 누르면 메인 피드에서 빠지고 접힌
  "히스토리" 섹션으로만 남음(22차); 동료 그룹 참여 상태면 코드 카드 + "동료에게 지금 공유하기" 버튼
  (21차, Web Share API). Manager Dashboard "팀 액션·공지"에서 참여 코드 커스터마이즈(PRO — 20차)와
  팀 초대 코드/링크 복사, "근무 일정 관리"에서 팀 로스터 확인/배정(직원이 스스로 입력하거나 동료끼리
  교대한 근무도 그대로 보임) + 근무 교대 알림(읽기 전용 — 승인 권한 없음), 좌측 상단 드롭다운으로
  강남점/잠실점(준비 중) 전환)
- 샘플 캘린더 파일: `shyftstarter-근무일정.ics` — 실제 내보내기 기능으로 생성된 파일, 구글/애플/아웃룩
  캘린더에 바로 가져오기 테스트 가능
- 라이브 프리뷰: Artifact 퍼블리시 (같은 URL로 계속 업데이트 중, 대화 카드 참조 — 단 캘린더 내보내기는
  iframe 다운로드 제약으로 이 프리뷰에서는 다운로드가 보이지 않을 수 있고, 브라우저 알림·Web Share
  API도 뷰어의 권한/환경 설정에 따라 프리뷰에서는 지원되지 않을 수 있음 — 이 경우에도 토스트나 클립보드
  복사로는 항상 확인 가능)
- 전략 문서: `claude/shyftstarter-v2-strategy-b2c-pivot.md` — v2 Shift Companion 전략 확정본

## 다음에 논의/진행할 것
- **Team 탭 "확인" 상태가 지금은 로컬(브라우저) 상태일 뿐** (22차) — `readFeedIds`가
  `AppStateProvider`에 있어 탭을 오가도 유지되지만, 실제 백엔드가 없어 새로고침하거나 다른 기기로
  접속하면 초기화된다. 실서비스에서는 사용자별 서버 상태로 영속화 필요(다른 개인화 상태들과 동일한
  다음 단계 — 아래 백엔드 연동 항목과 함께 처리)
- **온보딩 랜딩 화면이 매번 로그인마다 다시 보이는 문제** (22차) — 지금은 로그아웃(데모 재시작) 시엔
  건너뛰지만, 실제 앱이라면 "한 번 본 사용자에게는 다시 안 보이기"를 로컬 스토리지나 서버의 사용자
  프로필 플래그로 기억해야 함. 지금은 데모 편의상 앱을 새로고침하면 다시 온보딩부터 시작됨
  (`useState` 초기값이라 영속화되지 않음)
- **예상 급여 계산기의 "추가 수당" 기록이 기간 구분 없이 누적되는 점** (21차) — 지금은 공휴수당/연장수당
  기록에 날짜나 주차 구분이 없어 모두 하나의 목록에 누적되고 총액에 항상 더해진다. 실사용 시 "이번 달
  근무한 것만 계산"처럼 기간별로 나눠 봐야 할 필요가 생기면 `ExtraPayEntry`에 날짜/주차 필드를 추가하는
  확장이 필요
- **예상 급여 계산기가 `mockData.shifts`(8/22~9/3 범위)만 커버** (21차) — My Shift 메인 화면과 같은
  데이터 소스를 쓰기 때문에, 이 범위를 벗어나는 "이번 달 전체" 같은 계산은 지금 구조로는 안 됨.
  로스터/개인 시프트 데이터 소스 통합(아래 항목)이 해결되면 함께 넓어질 수 있음
- **동료 그룹 공유가 Web Share API 지원 여부에 좌우됨** (21차) — 데스크톱 브라우저 등 미지원 환경에서는
  자동으로 클립보드 복사로 대체되지만, "카카오톡 공유"라는 구체적 채널을 원한다면 카카오 SDK 연동
  (앱 키 발급 등 실제 백엔드/도메인 등록 필요)이 별도로 필요
- **반복 근무 등록이 "다른 한 주의 같은 요일"까지만 지원** (21차) — `ROSTER_WEEKS`가 2주치 데이터만
  갖고 있어 "매주 반복"이라는 무한 규칙 자체는 지원하지 않음. 실제 백엔드가 붙어 로스터가 임의 기간을
  다루게 되면, 반복 규칙(요일 선택 + 종료 조건)을 제대로 모델링하는 게 다음 단계
- **"공지 전원 작성 권한"이 동료 그룹의 피드 가시성까지 여는 것이었는지 확인 필요** (19차) — 이번
  라운드는 "다들 권한이 있게"를 "이미 피드가 보이는 매장 멤버십 안에서 작성자 제한을 푸는 것"으로 좁게
  해석했다. 만약 동료 그룹(`crew`) 멤버도 공지를 보고 남길 수 있어야 한다는 의도였다면, 18차에서 정한
  §9-1 티어 경계(공지·인수인계는 Team/매장 티어 전용) 자체를 다시 논의해야 함 — 다음 대화에서 확인
- **동료 그룹 → 매장 "승격/흡수" 플로우 설계** (18차, 전략 문서 §9-1) — 지금은 동료 그룹(`crew`)과
  매장(`store`)이 완전히 독립된 엔티티라, 진짜 매니저가 나중에 매장을 개설해도 자동으로 연결되지
  않는다. 누가 트리거하나(매니저? 시스템이 매칭 제안?), 멤버 매칭을 자동으로 할지 매니저가 수동
  확인할지 등 실제 백엔드가 붙을 때 우선 설계가 필요
- **매니저 알림을 실제 알림/푸시로 보낼지** — 지금은 "교대 완료 · 자동 반영됨" 카드가 매니저 화면에
  뜨는 것으로 알림을 표현하지만, 매니저가 그 화면을 보고 있지 않으면 놓친다. 실제로는 근무 시작
  알림과 마찬가지로 서비스워커/푸시가 필요한 항목 — 근무 시작 알림 이슈와 함께 백엔드 붙을 때 처리
- 동료 승인제 전환에 따라 **매니저가 완전히 배제되는 게 맞는지(정책적으로), 아니면 "이상 징후"(예:
  같은 사람이 반복적으로 교대를 몰아준다거나 노동법상 문제 소지가 있는 조합)만 매니저에게 플래그하는
  중간 단계를 둘지** — 지금은 완전 무권한. 실사용 피드백에 따라 "사후 취소권" 같은 안전장치를 추가할지
  결정 필요
- **로스터(`roster.ts`)와 지은 개인 시프트(`mockData.shifts`)/캘린더 내보내기/예상 급여 계산기 데이터
  소스 통합** — 14차부터 이어진 알려진 seam이 21차 예상 급여 계산기까지 이어졌다. 근무 교대·셀프 근무
  입력 모두 `roster`에는 정확히 반영되지만 My Shift 메인 화면의 "예정된/지난 근무" 목록, .ics 내보내기,
  예상 급여 계산기는 모두 여전히 `mockData.shifts`를 그대로 쓴다. 두 데이터를 하나로 합치거나
  `mockData.shifts`를 `roster` 파생값으로 재계산하는 방식으로 다음 라운드에서 해소 필요 — 지금
  우선순위가 가장 높은 정리 항목
- 근무 시작 알림을 실제 예약 푸시로 동작시키려면 서비스워커 + 백엔드(FCM/APNs 등)가 필요 — 지금은
  탭이 열려 있을 때 커스텀 리마인더만 진짜로 동작하고, 근무 시작 알림은 설정/문구 + "지금 테스트"까지만
- 근무 교대 요청을 상호 교환이 아닌 "양도(giveaway)"형(대상자가 그냥 대신 맡아주는, 맞바꿀 근무가
  없는 경우)까지 지원할지 — 지금은 반드시 양쪽 다 예정된 근무가 있어야 요청 가능
- 매장 전환(13차)을 실제 멀티 매장으로 확장할지 — 지금은 잠실점이 UI 예시만 있고 실제 팀/근무/액션
  데이터가 없음. 두 번째 매장 데이터를 채울지, 아니면 실제 Business/Enterprise 티어에서만 의미 있는
  기능이니 그때 가서 진짜 데이터 모델을 설계할지 결정 필요
- QR 초대(Business 티어) 실제 구현 시점 — 지금은 Manager 화면에 잠금 상태로 UI만 존재
- 코드 만료/재발급, 퇴사자 코드 무효화 등 초대 코드 관리 기능 — 20차로 매니저가 코드를 직접 바꿀 수는
  있게 됐지만, 자동 만료·이력 관리(예전 코드로 참여 시도한 사람에게 안내)는 아직 없음
- Free → Team 전환 트리거의 구체적 기준(매장 내 몇 명이 코드로 이미 들어와 있을 때 매니저에게
  업그레이드를 제안할지) — 이제 "코드로 들어온 인원 수"를 셀 수 있는 데이터 기반은 마련됨, 트리거 값
  자체는 실사용 후 결정
- 실제 OAuth 소셜 로그인 연동 시점 — 각 사 개발자 계정 개설, 백엔드 토큰 교환 로직 필요 (지금은 데모 UX만)
- 캘린더 실시간 양방향 동기화(구글 캘린더 API 등) 도입 여부/시점 — 지금은 일회성 .ics 내보내기까지만
- 실제 백엔드 연동 여부/시점 (Supabase 등), 실제 Claude API 연동 시점(`generateQuickActions()` 등 교체)
- 인증/로그인 + Role 기반 접근 제어를 실제로 강제하는 백엔드 (지금은 프론트에서 화면만 분리)
- Will 점수 계산식(4분면 임계값 등) 실제 데이터로 검증
- 리더보드 On/Off는 현재 화면 내 로컬 토글(데모) — 실서비스에서는 기업/매장 단위 관리자 설정으로 이전 필요
- Brand Alignment 지표는 개념 단계 — 고객사 브랜드 가이드라인 온보딩 후 정의 확정 필요
- 상위 티어(Business/Enterprise) 진입 시 숨겨진 기능(Stats/Coach/Progress/리더보드/Executive
  Dashboard)을 nav에 다시 연결하는 게이팅 로직 설계
- 경쟁 분석(11차)을 전략 문서 별도 섹션으로 formalize할지 여부
