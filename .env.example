# Copy to .env (never commit .env). Both empty → the app runs on the on-device
# adapter with seeded demo data (what TestFlight testers get before the backend
# is connected).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
